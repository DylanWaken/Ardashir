(function () {
  "use strict";

  const terms = [
    ["rhi", "RHI", "Graphics", "A rendering hardware interface: an abstraction over graphics APIs and GPU devices.", "Ardashir uses an RHI boundary so rendering code can target multiple native graphics APIs.", ["nvrhi", "backend", "device"]],
    ["nvrhi", "NVRHI", "Project", "NVIDIA Rendering Hardware Interface, a portable C++ graphics abstraction.", "ArdaBackend adapts NVRHI objects and commands for the rest of Ardashir.", ["rhi", "d3d12", "vulkan"]],
    ["backend", "Backend", "Project", "The implementation layer that translates engine-facing operations to a lower-level system.", "ArdaBackend owns the graphics abstraction boundary, resource creation, shader integration, and pipeline setup.", ["rhi", "nvrhi", "native-import"]],
    ["device", "Device", "Graphics", "The logical interface to a GPU used to create resources, pipelines, and command contexts.", "ArdaBackend device objects are the root for most NVRHI allocations and capability queries.", ["queue", "command-list", "device-affinity"]],
    ["command-list", "Command list", "Graphics", "A recorded sequence of GPU commands submitted together for execution.", "ArdaBackend records resource transitions, draws, dispatches, and copies into NVRHI command lists.", ["queue", "barrier", "resource-state"]],
    ["queue", "Queue", "Graphics", "An ordered GPU submission stream, commonly specialized for graphics, compute, or copy work.", "ArdaBackend submits command lists to queues exposed by its device integration.", ["command-list", "synchronization", "fence"]],
    ["resource", "Resource", "Graphics", "GPU-visible storage or an object that owns such storage, such as a buffer or texture.", "ArdaBackend resources carry descriptions, state, ownership, and native interoperability information.", ["resource-state", "texture-array", "format"]],
    ["descriptor", "Descriptor", "Graphics", "A small record that tells a GPU how a shader or pipeline may access a resource.", "ArdaBackend uses descriptors through NVRHI binding layouts and binding sets.", ["binding-layout", "binding-set", "bindless"]],
    ["binding-layout", "Binding layout", "Graphics", "A declaration of the resource slots and access types expected by shaders.", "ArdaBackend creates NVRHI binding layouts from shader and pipeline requirements.", ["descriptor", "binding-set", "parameter-metadata-reflection"]],
    ["binding-set", "Binding set", "Graphics", "Concrete resources bound to the slots declared by a binding layout.", "ArdaBackend assembles NVRHI binding sets for command recording and pipeline execution.", ["binding-layout", "srv", "uav"]],
    ["shader", "Shader", "Graphics", "A program executed by a programmable GPU stage.", "ArdaBackend loads precompiled DXIL or SPIR-V artifacts and creates shader objects; compilation is offline and current binding metadata is authored in C++ rather than recovered through binary reflection.", ["shader-stage", "bytecode", "global-shader"]],
    ["shader-stage", "Shader stage", "Graphics", "A programmable phase of a graphics or compute pipeline, such as vertex, pixel, or compute.", "ArdaBackend maps project shader-stage declarations to NVRHI and native API stages.", ["shader", "pipeline", "mesh-shader"]],
    ["bytecode", "Bytecode", "Computer science", "A compiled, machine-consumable representation of a program.", "Shader bytecode enters ArdaBackend as DXIL or SPIR-V and is used to create native shader objects.", ["dxil", "spir-v", "shader"]],
    ["dxil", "DXIL", "Graphics", "DirectX Intermediate Language, the compiled shader representation used by modern Direct3D.", "The D3D12 path consumes DXIL shader bytecode.", ["bytecode", "d3d12", "spir-v"]],
    ["spir-v", "SPIR-V", "Graphics", "A standardized intermediate language for graphics and compute shaders.", "The Vulkan path consumes SPIR-V shader bytecode.", ["bytecode", "vulkan", "dxil"]],
    ["virtual-shader-path", "Virtual shader path", "Project", "A stable logical shader location resolved independently of a physical filesystem path.", "ArdaBackend maps virtual shader roots to source directories so includes and shader identities remain portable.", ["shader", "global-shader", "cache"]],
    ["global-shader", "Global shader", "Project", "A shader type registered independently of a material or scene object.", "ArdaBackend global shaders use registered type information, virtual paths, and parameter metadata.", ["virtual-shader-path", "parameter-metadata-reflection", "singleton"]],
    ["parameter-metadata-reflection", "Parameter metadata / reflection", "Project", "Structured information describing fields, types, offsets, and bindings without executing the program.", "ArdaBackend currently uses authored C++ parameter metadata to validate and construct bindings; binary shader reflection is not the runtime source of this layout and can only serve as an external validation technique.", ["binding-layout", "shader", "abi"]],
    ["push-constants", "Push constants", "Graphics", "A small block of values written directly into command state without a separate buffer binding.", "ArdaBackend maps compact per-dispatch or per-draw values to the native mechanism supported by NVRHI.", ["constant-buffer", "binding-layout", "command-list"]],
    ["pipeline", "Pipeline", "Graphics", "The configured sequence of GPU stages and fixed-function state used to process work.", "ArdaBackend builds graphics, compute, ray-tracing, and mesh pipelines through NVRHI.", ["pso", "shader-stage", "framebuffer"]],
    ["pso", "PSO", "Graphics", "Pipeline state object: an immutable compiled bundle of shaders and fixed-function settings.", "ArdaBackend caches PSOs to avoid repeated expensive pipeline creation.", ["pipeline", "cache", "immutable"]],
    ["framebuffer", "Framebuffer", "Graphics", "The set of image attachments targeted by a rendering pass.", "ArdaBackend framebuffer descriptions identify render targets and optional depth/stencil attachments.", ["render-target", "format", "sample-count-msaa"]],
    ["render-target", "Render target", "Graphics", "A texture view that receives output from a rendering operation.", "ArdaBackend describes render-target formats when creating framebuffers and graphics pipelines.", ["framebuffer", "texture-array", "format"]],
    ["swap-chain", "Swap chain", "Graphics", "A sequence of presentable images exchanged between rendering and a display surface.", "ArdaBackend imports or wraps swap-chain images for rendering and presentation.", ["render-target", "native-import", "synchronization"]],
    ["barrier", "Barrier", "Graphics", "An ordering and visibility operation that makes resource usage transitions safe.", "ArdaBackend emits or tracks NVRHI barriers when resources change state or access pattern.", ["resource-state", "synchronization", "command-list"]],
    ["resource-barrier", "Resource barrier", "Graphics", "A barrier that orders accesses to a resource or transitions it between declared usage states.", "ArdaBackend resource barriers make command-list state transitions explicit; see the broader barrier contract.", ["barrier", "resource-state", "command-list"]],
    ["resource-state", "Resource state", "Graphics", "The declared way a GPU resource is currently used, such as copy destination or shader resource.", "ArdaBackend state tracking ensures accesses are compatible and inserts required transitions.", ["barrier", "resource", "validation-layer"]],
    ["synchronization", "Synchronization", "Computer science", "Coordination that establishes safe ordering and visibility between concurrent operations.", "ArdaBackend coordinates CPU work, queue submissions, and cross-queue resource use.", ["fence", "semaphore", "barrier"]],
    ["fence", "Fence", "Graphics", "A monotonically signaled synchronization primitive commonly observed by the CPU.", "ArdaBackend uses fences to determine when submitted GPU work and associated resources are safe to reuse.", ["synchronization", "queue", "lifetime"]],
    ["semaphore", "Semaphore", "Computer science", "A synchronization primitive used to signal availability or order work between execution contexts.", "Native backend integrations may use semaphores for queue-to-queue or presentation synchronization.", ["synchronization", "fence", "vulkan"]],
    ["intrusive-reference-counting", "Intrusive reference counting", "Computer science", "Shared ownership where the reference count is stored inside the managed object.", "NVRHI objects exposed through ArdaBackend may use intrusive references; callers must retain handles for the required lifetime.", ["ownership", "lifetime", "raii"]],
    ["device-affinity", "Device affinity", "Graphics", "The association of work or data with one GPU in a multi-device system.", "ArdaBackend preserves device identity so resources and commands are used with the device that created them.", ["device", "resource", "native-import"]],
    ["cache", "Cache", "Computer science", "Stored reusable results that avoid repeating expensive work.", "ArdaBackend caches pipeline state and other derived objects by stable descriptors or hashes.", ["lru", "hash", "pso"]],
    ["lru", "LRU", "Computer science", "Least recently used, an eviction policy that discards the item unused for the longest time.", "An LRU policy can bound ArdaBackend caches while favoring recently reused pipeline objects.", ["cache", "lifetime", "hash"]],
    ["hash", "Hash", "Computer science", "A fixed-size value derived from data for fast lookup or change detection.", "ArdaBackend uses hashes as cache keys only when all state affecting the result is represented.", ["cache", "immutable", "pso"]],
    ["immutable", "Immutable", "Computer science", "Unable to change after construction.", "Immutable descriptors and pipeline objects make cache identity and concurrent reuse easier to reason about.", ["pso", "hash", "threading"]],
    ["ownership", "Ownership", "Computer science", "The responsibility for retaining, releasing, or destroying an object.", "ArdaBackend APIs distinguish borrowed native handles, retained NVRHI references, and newly created objects.", ["lifetime", "raii", "intrusive-reference-counting"]],
    ["lifetime", "Lifetime", "Computer science", "The interval during which an object exists and may be safely accessed.", "GPU work can extend effective resource lifetime beyond the CPU call that submits it.", ["ownership", "fence", "raii"]],
    ["singleton", "Singleton", "Computer science", "A design in which one globally accessible instance represents a service or registry.", "Some Ardashir registries use singleton-like access; initialization and shutdown order remain part of their contract.", ["lifetime", "global-shader", "threading"]],
    ["raii", "RAII", "Computer science", "Resource acquisition is initialization: C++ object lifetime controls resource ownership and cleanup.", "ArdaBackend wrappers use destructors and reference objects to release resources predictably.", ["ownership", "lifetime", "intrusive-reference-counting"]],
    ["abi", "ABI", "Computer science", "Application binary interface: the calling, layout, and linkage rules shared by compiled components.", "Shader parameter layouts and native interop must preserve the ABI expected across CPU, shader, and library boundaries.", ["api", "parameter-metadata-reflection", "format"]],
    ["api", "API", "Computer science", "Application programming interface: the named operations and data contracts exposed to callers.", "ArdaBackend's public headers form its source-level API over NVRHI and native graphics integrations.", ["abi", "backend", "validation-layer"]],
    ["validation-layer", "Validation layer", "Graphics", "A diagnostic layer that checks API usage and reports contract violations.", "D3D12 debug and Vulkan validation layers help diagnose incorrect states, descriptors, and synchronization around ArdaBackend.", ["d3d12", "vulkan", "resource-state"]],
    ["d3d12", "D3D12", "Graphics", "Direct3D 12, Microsoft's explicit low-level graphics and compute API.", "One ArdaBackend native path creates or imports NVRHI objects backed by D3D12.", ["nvrhi", "dxil", "native-import"]],
    ["vulkan", "Vulkan", "Graphics", "A cross-platform explicit graphics and compute API from the Khronos Group.", "One ArdaBackend native path creates or imports NVRHI objects backed by Vulkan.", ["nvrhi", "spir-v", "native-import"]],
    ["viewport", "Viewport", "Graphics", "A transform that maps normalized device coordinates into a rectangular render-target region.", "ArdaBackend records viewport state with graphics commands and pipeline-compatible dimensions.", ["scissor", "render-target", "pipeline"]],
    ["scissor", "Scissor", "Graphics", "A rectangle that rejects rasterized fragments outside its bounds.", "ArdaBackend records scissor rectangles alongside viewport and raster state.", ["viewport", "render-target", "pipeline"]],
    ["vertex-buffer", "Vertex buffer", "Graphics", "A buffer containing per-vertex or per-instance input data for drawing.", "ArdaBackend binds vertex buffers according to pipeline input layout and draw parameters.", ["index-buffer", "resource", "command-list"]],
    ["index-buffer", "Index buffer", "Graphics", "A buffer of indices selecting vertices for indexed drawing.", "ArdaBackend binds an index format and buffer before indexed draw commands.", ["vertex-buffer", "format", "command-list"]],
    ["uav", "UAV", "Graphics", "Unordered access view: a resource view that permits shader reads and writes without fixed ordering.", "ArdaBackend exposes UAV bindings through NVRHI descriptors and binding sets.", ["srv", "constant-buffer", "barrier"]],
    ["srv", "SRV", "Graphics", "Shader resource view: a read-oriented interpretation of a buffer or texture for shaders.", "ArdaBackend exposes SRV bindings through NVRHI descriptors and binding sets.", ["uav", "descriptor", "binding-set"]],
    ["constant-buffer", "Constant buffer", "Graphics", "A buffer that supplies small, read-only uniform values to shader invocations.", "ArdaBackend maps parameter data to constant-buffer bindings where push constants are unsuitable.", ["push-constants", "binding-set", "shader"]],
    ["ray-tracing", "Ray tracing", "Graphics", "Rendering or computation based on tracing rays through geometric acceleration structures.", "ArdaBackend exposes NVRHI ray-tracing pipelines, shader tables, and acceleration-structure operations when supported.", ["acceleration-structure", "pipeline", "shader-stage"]],
    ["acceleration-structure", "Acceleration structure", "Graphics", "A hierarchy optimized for fast spatial intersection queries.", "ArdaBackend creates and builds bottom- and top-level structures used by ray-tracing commands.", ["ray-tracing", "resource", "barrier"]],
    ["mesh-shader", "Mesh shader", "Graphics", "A programmable geometry stage that emits mesh primitives without the traditional vertex-input pipeline.", "ArdaBackend exposes mesh-pipeline creation where the active NVRHI backend supports it.", ["shader-stage", "pipeline", "vertex-buffer"]],
    ["bindless", "Bindless", "Graphics", "A resource-binding model where shaders index large descriptor collections dynamically.", "ArdaBackend can represent bindless layouts through NVRHI when device capabilities permit.", ["descriptor", "binding-layout", "device"]],
    ["mip-level", "Mip level", "Graphics", "One resolution level in a texture's prefiltered image pyramid.", "ArdaBackend resource views select mip ranges for sampling, rendering, copies, and barriers.", ["texture-array", "resource-state", "format"]],
    ["texture-array", "Texture array", "Graphics", "A texture containing multiple same-sized image layers addressed by an array index.", "ArdaBackend views can select array slices and mip levels as render targets or shader resources.", ["mip-level", "render-target", "srv"]],
    ["format", "Format", "Graphics", "The component layout, bit width, and numeric interpretation of stored image or buffer data.", "ArdaBackend requires compatible formats across resources, views, framebuffers, and pipelines.", ["render-target", "framebuffer", "abi"]],
    ["sample-count-msaa", "Sample count / MSAA", "Graphics", "The number of coverage samples stored per pixel; multisample anti-aliasing uses more than one.", "ArdaBackend keeps pipeline and framebuffer sample counts compatible.", ["framebuffer", "render-target", "pipeline"]],
    ["external-device-provider", "External device provider", "Project", "A host-owned adapter that supplies an existing native graphics device and queues to an abstraction layer.", "ArdaBackend copies the provider's D3D12 or Vulkan descriptor and optional lifetime token during initialization; callbacks are serialized and non-reentrant.", ["native-import", "lifetime-token", "queue-coordination"]],
    ["lifetime-token", "Lifetime token", "Computer science", "A shared ownership object retained to keep a separate host lifetime valid without owning the native handle directly.", "External Arda device and resource wrappers retain optional tokens; one native resource identity must use one canonical token ownership identity.", ["ownership", "lifetime", "native-import"]],
    ["queue-coordination", "Queue coordination", "Graphics", "Host-side serialization that grants one integration an ordered, exclusive interval on a shared native submission queue.", "External Arda integrations must coordinate supplied queues around recording, submission, and retirement because no submission-hook API interleaves an independent host RHI thread.", ["queue", "synchronization", "external-device-provider"]],
    ["native-import", "Native import", "Project", "Wrapping an externally created native graphics object for use through an abstraction layer.", "ArdaBackend imports selected D3D12 or Vulkan textures and buffers from truthful descriptors, retains optional lifetime tokens, and leaves host ownership and synchronization explicit.", ["d3d12", "vulkan", "lifetime-token"]],
    ["arda-render-graph", "ArdaRenderGraph", "Project", "Ardashir's system for declaring render passes, resources, and their dependencies before execution.", "ArdaRenderGraph schedules higher-level work that is ultimately materialized through ArdaBackend commands and resources.", ["render-graph", "backend", "pass"]],
    ["render-graph", "Render graph", "Graphics", "A directed graph that models rendering work as passes and resource dependencies so an engine can analyze execution before recording commands.", "ArdaRenderGraph builds this model during registration, compiles hazards, culling, queues, transitions, and lifetimes, then records and submits the surviving passes.", ["arda-render-graph", "dag", "dependency-edge"]],
    ["dag", "DAG", "Computer science", "Directed acyclic graph: vertices connected by one-way edges with no path that returns to its starting vertex.", "ArdaRenderGraph requires every dependency to point forward in stable handle order, making registration order a topological order and rejecting cycles by construction.", ["dependency-edge", "topological-order", "registration-order"]],
    ["dependency-edge", "Dependency edge", "Computer science", "A directed relation stating that one operation must precede another because of data flow or an explicit ordering constraint.", "ArdaRenderGraph records data edges for producer reachability and separate synchronization-only edges for ordering that must not keep otherwise dead work live.", ["producer", "consumer", "raw-war-waw-hazards"]],
    ["producer", "Producer", "Computer science", "An operation that creates or writes a value consumed by later work.", "In ArdaRenderGraph, a producer pass precedes a consumer through a data edge; the prologue acts as the initial producer for imported resources.", ["consumer", "dependency-edge", "prologue"]],
    ["consumer", "Consumer", "Computer science", "An operation that reads or otherwise depends on a value produced by earlier work.", "ArdaRenderGraph uses consumer relationships to derive ordering, culling reachability, and cross-queue waits.", ["producer", "dependency-edge", "queue-dependency"]],
    ["pass", "Pass", "Graphics", "A named unit of rendering or compute work with declared resource accesses and an execution callback.", "ArdaRenderGraph registers passes first, validates their declarations, may cull dead passes, and executes live callbacks with validated resource access.", ["render-graph", "raster-pass", "culling"]],
    ["liveness", "Liveness", "Computer science", "The property that work or data can still contribute to an observable result and therefore must be retained.", "ArdaRenderGraph traces data producers backward from the epilogue and NeverCull roots, then rebuilds resource live intervals using only surviving passes.", ["culling", "epilogue", "transient-resource"]],
    ["culling", "Culling", "Computer science", "Removing work proven unable to affect any required output.", "ArdaRenderGraph removes passes not reachable through data-producer edges from observable roots; synchronization-only edges do not preserve liveness, while immediate mode keeps every pass.", ["liveness", "dependency-edge", "immediate-mode"]],
    ["transient-resource", "Transient resource", "Graphics", "A temporary resource whose storage is needed only for a bounded interval of one workload or frame.", "ArdaRenderGraph computes live intervals and can recycle compatible committed resources within one queue domain; portable placed-memory aliasing is currently disabled.", ["liveness", "resource-aliasing", "physical-resource"]],
    ["resource-aliasing", "Resource aliasing", "Graphics", "Reusing the same physical memory for distinct logical resources whose live intervals do not overlap.", "ArdaRenderGraph computes an ideal transient layout but does not currently apply physical placed-resource aliasing because the RHI lacks portable aliasing barriers and heap-compatibility queries.", ["logical-resource", "physical-resource", "transient-resource"]],
    ["logical-resource", "Logical resource", "Graphics", "A graph-level resource identity that describes intended data and usage without necessarily owning a unique GPU allocation.", "ArdaRenderGraph registers logical textures, buffers, views, and acceleration structures, then materializes backing objects only after compilation.", ["physical-resource", "transient-resource", "extraction"]],
    ["physical-resource", "Physical resource", "Graphics", "The concrete GPU object or memory allocation that realizes one or more logical resource identities.", "ArdaRenderGraph materializes RHI resources before recording; compatible transient logical resources may reuse committed objects, but physical placed-memory aliasing remains disabled.", ["logical-resource", "resource-aliasing", "resource"]],
    ["extraction", "Extraction", "Project", "Publishing a graph-managed resource for use after graph execution, which makes that resource an observable output.", "ArdaRenderGraph keeps extracted resources live through the epilogue, transitions them to their declared final state, and assigns output references after submission; extraction does not imply GPU completion.", ["epilogue", "liveness", "fence"]],
    ["prologue", "Prologue", "Project", "A synthetic first graph node representing state and dependencies entering a workload.", "ArdaRenderGraph creates the prologue first and treats it as the initial producer for imported resources; it runs on the graphics pipeline as a sentinel.", ["epilogue", "producer", "registration-order"]],
    ["epilogue", "Epilogue", "Project", "A synthetic final graph node that anchors observable outputs and graph-exit state requirements.", "ArdaRenderGraph appends the epilogue during compilation and connects external and extracted resources to it so required producers remain live and final transitions are emitted.", ["prologue", "extraction", "liveness"]],
    ["arena-allocator", "Arena allocator", "Computer science", "An allocator that serves many objects from larger blocks and releases them together, often running registered destructors at reset.", "ArdaRenderGraph stores graph records and pass parameters in a builder-scoped arena, preserving stable addresses and destroying non-trivial objects in reverse construction order.", ["lifetime", "registration-order", "raii"]],
    ["blackboard", "Blackboard", "Project", "A graph-scoped store used to share named or typed values between otherwise separate setup code.", "ArdaRenderGraph's blackboard stores at most one copy-constructible value per C++ type for the lifetime of a builder.", ["render-graph", "lifetime", "arena-allocator"]],
    ["topological-order", "Topological order", "Computer science", "A linear ordering of a DAG in which every producer appears before each of its consumers.", "ArdaRenderGraph accepts only forward dependency edges, so its deterministic execution order is the registration-order subsequence left after culling rather than a separately sorted order.", ["dag", "registration-order", "dependency-edge"]],
    ["raw-war-waw-hazards", "RAW / WAR / WAW hazards", "Computer science", "Read-after-write, write-after-read, and write-after-write ordering conflicts that arise when operations access the same data.", "ArdaRenderGraph models RAW and WAW hazards as producer data edges; WAR hazards become synchronization-only edges so a dead reader does not make an otherwise dead producer live.", ["dependency-edge", "synchronization", "resource-state"]],
    ["raster-pass", "Raster pass", "Graphics", "A graphics pass that rasterizes primitives into color or depth/stencil attachments.", "ArdaRenderGraph identifies raster passes from declared flags and attachments, forms compatible raster groups, and can let a SkipRenderPass callback manage framebuffer behavior explicitly.", ["pass", "framebuffer", "render-target"]],
    ["async-compute", "Async compute", "Graphics", "Compute work submitted to a compute-capable queue so it may overlap independent graphics work.", "ArdaRenderGraph selects async compute only outside immediate mode when the queue exists and all declared states are compatible; otherwise the pass falls back to graphics.", ["queue", "queue-dependency", "immediate-mode"]],
    ["queue-dependency", "Queue dependency", "Graphics", "A dependency whose producer and consumer execute on different GPU queues and therefore requires an inter-queue wait or signal.", "ArdaRenderGraph derives these records in consumer execution order and issues queue waits using the submitted producer instance before submitting the consumer.", ["producer", "consumer", "synchronization"]],
    ["clobber", "Clobber", "Project", "A diagnostic overwrite with a conspicuous pattern used to expose code that relies on uninitialized or unexpectedly preserved data.", "With first-write clobbering enabled, ArdaRenderGraph clears safely supported non-external textures and buffers immediately before their first write; unsupported pass and resource cases are skipped.", ["validation-layer", "transient-resource", "resource"]],
    ["immediate-mode", "Immediate mode", "Project", "A correctness-oriented execution mode that favors simple serial behavior over normal scheduling and optimization.", "ArdaRenderGraph immediate mode keeps every pass, uses the graphics queue, extends resource lifetimes, and records and submits serially; it is a diagnostic oracle, not the normal optimized path.", ["culling", "async-compute", "liveness"]],
    ["registration-order", "Registration order", "Project", "The stable sequence in which graph objects or passes are added to a builder.", "ArdaRenderGraph permits only forward edges, so registration order is already topological; after culling, execution preserves the same relative order for deterministic behavior.", ["topological-order", "dag", "pass"]],
    ["threading", "Threading", "Computer science", "The division of work among independently scheduled execution threads.", "ArdaBackend contracts state which objects may be created, read, recorded, or submitted concurrently.", ["immutable", "synchronization", "singleton"]],
    ["vector", "Vector", "Mathematics", "An ordered set of components representing a direction, displacement, or point.", "Shaders pass vectors through parameter layouts and interpret them in an explicitly chosen coordinate space.", ["matrix", "transform", "abi"]],
    ["matrix", "Matrix", "Mathematics", "A rectangular array of values used to represent linear mappings and related computations.", "ArdaBackend preserves matrix memory layout across C++ parameter metadata and shader ABI boundaries.", ["vector", "transform", "parameter-metadata-reflection"]],
    ["transform", "Transform", "Mathematics", "A mapping that changes coordinates between spaces, commonly represented by a matrix.", "Rendering shaders consume transforms prepared by higher-level systems; layout and multiplication convention must agree.", ["matrix", "vector", "constant-buffer"]],
    ["homogeneous-coordinates", "Homogeneous coordinates", "Mathematics", "A representation that adds a coordinate so translation and perspective can be expressed with matrix multiplication.", "Graphics shaders commonly use four-component positions and 4-by-4 transforms at the ArdaBackend parameter boundary.", ["matrix", "vector", "viewport"]]
  ];

  const glossary = {};
  for (const [slug, term, category, definition, project, related] of terms) {
    glossary[slug] = {
      id: "term-" + slug,
      slug,
      term,
      category,
      definition,
      project,
      related: related.map((relatedSlug) => ({
        slug: relatedSlug,
        href: "#term-" + relatedSlug
      }))
    };
  }

  window.ArdaGlossary = Object.freeze(glossary);
})();
