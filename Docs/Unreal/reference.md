# Unreal rendering atlas — text reference

Source: Unreal Engine 5.8.1, commit `71fe36aac5a8df5ccd66c763ffc902b29b6a9c43`. Local checkout: `D:/UnrealEngine`.

Generated by `Scripts/Docs/BuildUnrealGuide.py` from authored explanations and verified source anchors. This contains the pipeline walkthrough and general resource reference. The actual component/proxy inheritance and their GPU storage associations are in [primitive-reference.md](primitive-reference.md). Source paths are relative to the Unreal checkout, with one-based line numbers pinned to that commit. Explanations paraphrase responsibilities; engine implementation bodies are not reproduced.

## Scope and reading the diagrams

Explicit class/struct definitions and BEGIN_SHADER_PARAMETER_STRUCT-family declarations in the listed runtime rendering modules, plus rendering-related Engine headers. Includes inactive preprocessor alternatives. Test directories, plugins, editor/developer modules, enums, aliases, forward declarations, template instantiations, macro-definition templates and other macro-generated classes are excluded. This lexical inventory is not a compiler AST or a claim to enumerate every Unreal type.

The inventory contains 9,512 declarations in 2,485 files; 147 key types have authored explanations. Modules: RHI, RHICore, RenderCore, Renderer, D3D12RHI, VulkanRHI, MetalRHI, OpenGLDrv, NullDrv, Landscape, Engine.

The interactive scene page has actual C++ inheritance trees rooted at UPrimitiveComponent and FPrimitiveSceneProxy. Every parent-child edge follows a uniquely resolved declared base, including across module/plugin boundaries. Filtering retains original ancestors. All bases, including multiple inheritance and external interfaces, remain in the inspector. GPU storage and component/proxy associations appear separately from inheritance. The class reference is a flat searchable inventory. The sections below group reference explanations by responsibility; those headings are not class hierarchy nodes.

The pipeline describes desktop deferred rendering with Nanite, Lumen GI/reflections and VSM enabled by default. Hardware/software tracing, async availability, material/platform support and show flags change the path. Mobile, forward rendering, path tracing, editor-only passes, plugins and every experimental mode are outside the detailed walkthrough. Column spacing is explanatory, not a measured duration or guaranteed serial GPU order. Arrows show selected major prerequisites, not every RDG edge. Conditional prerequisites are skipped when their feature is off; conventional paths remain.

## Cross-cutting contracts

### Three scene representations

FScene is persistent CPU renderer state; FGPUScene manages shader-readable scene tables; FRayTracingScene prepares instances/TLAS. None is an alias for either of the others. Nanite visibility, Lumen cards and VSM pages are additional purpose-specific representations.

### Three lifetimes

UObjects/components belong to gameplay/asset lifetime, proxies/render resources to rendering lifetime, and RHI allocations to retained GPU work. FRDG handles belong to their graph; persistent histories use extraction and later import. A C++ inheritance edge does not prove ownership, thread affinity or GPU residency.

### Nanite is geometry visibility

Nanite culls/rasterizes a hierarchical geometry representation and produces visibility/depth data. Material evaluation still writes a GBuffer or the configured Substrate representation, and deferred lighting still needs it. Nanite does not replace all mesh paths, materials, shadows or Lumen.

### Two Lumen caches

The Surface Cache parameterizes scene surfaces with cards and stores material/cached lighting atlases. The Radiance Cache stores world-space probe radiance for reuse. Screen probes are view-dependent gather samples. These are three distinct structures with different update and history rules.

### Tracing and lighting are separate choices

Screen traces can use only visible screen information. Remaining rays use the selected software distance-field or hardware triangle/AS representation. Surface-cache lookup versus hit lighting chooses how a hit is shaded; enabling hardware tracing does not mean every hit evaluates full material lighting.

### Representation mismatch

Nanite primary visibility can be more detailed than the geometry used by hardware ray tracing. Fallback meshes can create self-intersection/mismatch artifacts; screen traces may hide some discrepancies. Native Nanite ray tracing is a separate mode/coverage choice, not an unconditional property of enabling Nanite.

### Temporal correctness

Previous HZB, surface/radiance caches, probe/reflection histories and TSR history are separate inputs. Camera cuts, disocclusion, moved geometry/lights, resolution changes and residency changes require the appropriate rejection or invalidation. Keeping stale history is not equivalent to a valid cache hit.

### Schedule versus dependency

The timeline is a source-anchored dependency explanation, not a GPU capture or duration chart. CPU setup tasks and RDG passes can overlap. Async Lumen work must wait for its GBuffer/scene/AS inputs and must finish before consumers composite it; async compute does not guarantee a speedup.

### Reimplementation scope

The operation lists are an engineering decomposition inferred from source responsibilities. They are not a claim that Ardashir already implements Unreal/Nanite/Lumen, nor a complete binary-compatible port. Start with conventional deferred rendering and correctness tests, then add virtualized geometry, lighting caches and scheduling optimizations.

## General type reference by responsibility

### World, components & assets

Game-thread objects describe intent; render-thread proxies and GPU allocations have different lifetimes.

#### UWorld

- Kind: class; module: Engine.
- Declared bases: `UObject`, `FNetworkNotify`.
- Role: Owns the gameplay world and its link to the renderer's scene interface. A world is not itself a GPU allocation.
- Reimplementation responsibility: Keep simulation ownership separate from the render-thread scene; marshal registration, transforms and removal across the thread boundary.
- Source: `Engine/Source/Runtime/Engine/Classes/Engine/World.h:931`.

#### AActor

- Kind: class; module: Engine.
- Declared bases: `UObject`.
- Role: Groups components and gameplay behavior in a world. Renderable geometry normally reaches the renderer through primitive components rather than the actor itself.
- Reimplementation responsibility: Model transforms, component attachment and actor lifetime without making GPU passes dereference mutable actor state.
- Source: `Engine/Source/Runtime/Engine/Classes/GameFramework/Actor.h:282`.

#### UActorComponent

- Kind: class; module: Engine.
- Declared bases: `UObject`, `IInterface_AssetUserData`, `IAsyncPhysicsStateProcessor`.
- Role: Base for actor-owned behavior, including components with no spatial or rendering representation.
- Reimplementation responsibility: Implement component registration and destruction; only renderable subclasses need proxies.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/ActorComponent.h:160`.

#### USceneComponent

- Kind: class; module: Engine.
- Declared bases: `UActorComponent`.
- Role: Adds a transform and attachment hierarchy to an actor component. Spatial presence alone does not imply a drawable primitive.
- Reimplementation responsibility: Propagate attachment/transform changes and generate render updates only for descendants that need them.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/SceneComponent.h:88`.

#### UPrimitiveComponent

- Kind: class; module: Engine.
- Declared bases: `USceneComponent`, `INavRelevantInterface`, `IInterface_AsyncCompilation`, `IPhysicsComponent`, `FRenderAssetOwnerStreamingState`, `IPhysicsBodyInstanceOwner`, `IPhysicsBodyInstanceOwnerResolver`.
- Role: Represents a scene primitive with bounds, visibility and rendering settings; creates the render-thread primitive proxy.
- Reimplementation responsibility: Snapshot render-relevant state, allocate a stable scene identity, enqueue additions/updates/removal and defer destruction until consumers finish.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/PrimitiveComponent.h:307`.

#### UMeshComponent

- Kind: class; module: Engine.
- Declared bases: `UPrimitiveComponent`.
- Role: Adds material assignment and mesh-oriented behavior to primitive components.
- Reimplementation responsibility: Track material overrides independently from shared mesh asset buffers; invalidate cached draw/shader state when assignments change.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/MeshComponent.h:24`.

#### UStaticMeshComponent

- Kind: class; module: Engine.
- Declared bases: `UMeshComponent`.
- Role: Places a static-mesh asset in the world with per-component transform, material and rendering state.
- Reimplementation responsibility: Share asset render data between placements; create an appropriate ordinary or Nanite proxy according to support and settings.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/StaticMeshComponent.h:105`.

#### UInstancedStaticMeshComponent

- Kind: class; module: Engine.
- Declared bases: `UStaticMeshComponent`, `ISMInstanceManager`.
- Role: Represents many placements of one static mesh using instance data rather than one component per placement.
- Reimplementation responsibility: Maintain instance transforms, custom data, bounds and update ranges; upload changed instances to shader-readable scene storage.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/InstancedStaticMeshComponent.h:158`.

#### UHierarchicalInstancedStaticMeshComponent

- Kind: class; module: Engine.
- Declared bases: `UInstancedStaticMeshComponent`.
- Role: Adds hierarchical organization to instanced static meshes for efficient visibility and instance management.
- Reimplementation responsibility: Keep instance indices, bounds hierarchy and GPU instance data consistent through inserts/removals.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/HierarchicalInstancedStaticMeshComponent.h:135`.

#### USkinnedMeshComponent

- Kind: class; module: Engine.
- Declared bases: `UMeshComponent`, `ILODSyncInterface`, `IClothSimulationDataProvider`.
- Role: Base for mesh components whose vertices are transformed by a skinning/deformation system.
- Reimplementation responsibility: Produce current/previous deformation state, conservative bounds and matching raster/ray-tracing representations.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/SkinnedMeshComponent.h:267`.

#### USkeletalMeshComponent

- Kind: class; module: Engine.
- Declared bases: `USkinnedMeshComponent`, `IInterface_CollisionDataProvider`.
- Role: Connects skeletal animation and a skeletal mesh to skinned rendering.
- Reimplementation responsibility: Update bone/deformation buffers and velocities; ray tracing geometry may require separate BLAS updates after deformation.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/SkeletalMeshComponent.h:341`.

#### UStaticMesh

- Kind: class; module: Engine.
- Declared bases: `UStreamableRenderAsset`, `IInterface_CollisionDataProvider`, `IInterface_AssetUserData`, `IInterface_AsyncCompilation`.
- Role: Asset containing the static mesh's source/settings and runtime render data, including Nanite data when built.
- Reimplementation responsibility: Cook mesh LODs, vertex/index streams, material sections, bounds and optional Nanite/distance-field/card representations.
- Source: `Engine/Source/Runtime/Engine/Classes/Engine/StaticMesh.h:602`.

#### USkeletalMesh

- Kind: class; module: Engine.
- Declared bases: `USkinnedAsset`, `IInterface_CollisionDataProvider`, `IInterface_AssetUserData`, `INodeMappingProviderInterface`.
- Role: Asset holding skeletal mesh geometry, skeleton-related data and render LOD resources.
- Reimplementation responsibility: Build skinning streams, sections and LODs; distinguish immutable asset data from per-component animation state.
- Source: `Engine/Source/Runtime/Engine/Classes/Engine/SkeletalMesh.h:439`.

#### UMaterialInterface

- Kind: class; module: Engine.
- Declared bases: `UObject`, `IBlendableInterface`, `IInterface_AssetUserData`.
- Role: Common material-facing interface used by base materials and instances to expose rendering properties and parameters.
- Reimplementation responsibility: Resolve material identity and parameter inheritance to a render-thread material proxy.
- Source: `Engine/Source/Runtime/Engine/Public/Materials/MaterialInterface.h:351`.

#### UMaterial

- Kind: class; module: Engine.
- Declared bases: `UMaterialInterface`.
- Role: Authored material graph and shading configuration from which target-platform shader permutations are compiled.
- Reimplementation responsibility: Translate graph operations into shader code, derive shader keys and compile compatible material/vertex-factory/pass permutations.
- Source: `Engine/Source/Runtime/Engine/Public/Materials/Material.h:448`.

#### UMaterialInstance

- Kind: class; module: Engine.
- Declared bases: `UMaterialInterface`.
- Role: Material variant that inherits a parent material and supplies parameter/static-permutation overrides.
- Reimplementation responsibility: Share compiled permutations where possible and update uniform expressions when instance parameters change.
- Source: `Engine/Source/Runtime/Engine/Public/Materials/MaterialInstance.h:626`.

#### UTexture

- Kind: class; module: Engine.
- Declared bases: `UStreamableRenderAsset`, `IInterface_AssetUserData`, `IInterface_AsyncCompilation`.
- Role: Asset-level texture interface; its rendering resource is managed separately.
- Reimplementation responsibility: Stream/upload texture data through render resources and preserve mip residency and sampler/view contracts.
- Source: `Engine/Source/Runtime/Engine/Classes/Engine/Texture.h:1260`.

#### ULightComponent

- Kind: class; module: Engine.
- Declared bases: `ULightComponentBase`.
- Role: Base for light components that transfer light properties to the rendering scene.
- Reimplementation responsibility: Snapshot intensity, shape, channels and shadow policy into a light proxy and invalidate dependent caches on change.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/LightComponent.h:48`.

#### UDirectionalLightComponent

- Kind: class; module: Engine.
- Declared bases: `ULightComponent`.
- Role: Models a directional light with parallel illumination and directional shadow settings.
- Reimplementation responsibility: Build view-dependent shadow clipmaps/cascades and evaluate its BRDF contribution without treating it as a finite local-light sphere.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/DirectionalLightComponent.h:18`.

#### UPointLightComponent

- Kind: class; module: Engine.
- Declared bases: `ULocalLightComponent`.
- Role: Models an omnidirectional finite local light.
- Reimplementation responsibility: Cull affected receivers, evaluate attenuation and provide the selected shadow representation.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/PointLightComponent.h:19`.

#### USpotLightComponent

- Kind: class; module: Engine.
- Declared bases: `UPointLightComponent`.
- Role: Extends a point light with a cone-shaped emission region.
- Reimplementation responsibility: Add cone culling and angular attenuation to the local-light and shadow setup.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/SpotLightComponent.h:17`.

#### URectLightComponent

- Kind: class; module: Engine.
- Declared bases: `ULocalLightComponent`.
- Role: Models a rectangular area light.
- Reimplementation responsibility: Represent emitter shape and orientation; use the applicable area-light approximation or sampled lighting path.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/RectLightComponent.h:24`.

#### USkyLightComponent

- Kind: class; module: Engine.
- Declared bases: `ULightComponentBase`.
- Role: Describes environment illumination/capture settings rather than an ordinary local light volume.
- Reimplementation responsibility: Manage sky capture/convolution and make environment radiance available to indirect and reflection systems.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/SkyLightComponent.h:101`.

#### UDecalComponent

- Kind: class; module: Engine.
- Declared bases: `USceneComponent`.
- Role: Places a deferred decal with transform, material and projection bounds.
- Reimplementation responsibility: Classify decal blend requirements; schedule DBuffer decals before material evaluation or compatible later decals before lighting.
- Source: `Engine/Source/Runtime/Engine/Classes/Components/DecalComponent.h:24`.

### Render-thread scene

Persistent scene registration, proxies, primitive/light records and scene extensions.

#### FSceneInterface

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Engine-facing interface through which world/components communicate with a rendering scene.
- Reimplementation responsibility: Define scene registration and update operations without exposing private renderer storage to gameplay code.
- Source: `Engine/Source/Runtime/Engine/Public/SceneInterface.h:119`.

#### FScene

- Kind: class; module: Renderer.
- Declared bases: `FSceneInterface`.
- Role: Persistent renderer scene implementing FSceneInterface. Owns or coordinates primitive/light records, GPU Scene and feature-specific scene state.
- Reimplementation responsibility: Keep additions/removals, spatial indexing, cached draws and feature caches coherent. FScene is CPU renderer state, not the GPU Scene buffer.
- Source: `Engine/Source/Runtime/Renderer/Private/ScenePrivate.h:1624`.

#### FPrimitiveSceneProxy

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Render-thread representation of a primitive's immutable or explicitly updated drawing state. It supplies view relevance and mesh descriptions.
- Reimplementation responsibility: Avoid unsynchronized access to the live component; retain needed render data and generate static/dynamic mesh batches.
- Source: `Engine/Source/Runtime/Engine/Public/PrimitiveSceneProxy.h:291`.

#### FStaticMeshSceneProxy

- Kind: class; module: Engine.
- Declared bases: `FPrimitiveSceneProxy`.
- Role: Ordinary static-mesh primitive proxy that exposes mesh sections/LODs and their materials to renderer passes.
- Reimplementation responsibility: Select usable render LODs and produce mesh batches with correct material, vertex factory and primitive identity.
- Source: `Engine/Source/Runtime/Engine/Public/StaticMeshSceneProxy.h:34`.

#### FSkeletalMeshSceneProxy

- Kind: class; module: Engine.
- Declared bases: `FPrimitiveSceneProxy`.
- Role: Primitive proxy for skinned mesh rendering, connected to skeletal mesh render objects/resources.
- Reimplementation responsibility: Ensure mesh batches see the correct deformed vertex streams and current/previous frame data.
- Source: `Engine/Source/Runtime/Engine/Public/SkeletalMeshSceneProxy.h:21`.

#### Nanite::FSceneProxy

- Kind: class; module: Engine.
- Declared bases: `FSceneProxyBase`.
- Role: Nanite-specific primitive proxy that connects a component's scene identity and materials to Nanite resources/pipelines.
- Reimplementation responsibility: Register Nanite raster/shading bins and GPU Scene instances; provide non-Nanite representations when required by another feature.
- Source: `Engine/Source/Runtime/Engine/Public/NaniteSceneProxy.h:490`.

#### Nanite::FSceneProxyBase

- Kind: class; module: Engine.
- Declared bases: `FPrimitiveSceneProxy`.
- Role: Shared Nanite proxy base derived from FPrimitiveSceneProxy, bridging ordinary primitive contracts to Nanite-specific proxies.
- Reimplementation responsibility: Keep common primitive visibility/material/scene registration behavior available while specializing geometry and raster/shading resources.
- Source: `Engine/Source/Runtime/Engine/Public/NaniteSceneProxy.h:219`.

#### FPrimitiveSceneInfo

- Kind: class; module: Renderer.
- Declared bases: `FDeferredCleanupInterface`.
- Role: Renderer-owned registration record tying a primitive proxy to scene indices, cached mesh data and renderer bookkeeping.
- Reimplementation responsibility: Track stable identifiers and compact indices separately; invalidate cached commands and release records safely when a primitive leaves the scene.
- Source: `Engine/Source/Runtime/Renderer/Public/PrimitiveSceneInfo.h:269`.

#### FLightSceneProxy

- Kind: class; module: Engine.
- Declared bases: `FLightSceneProxyFixLayout`.
- Role: Render-thread snapshot of a light's shape, color, attenuation and shadow-related properties.
- Reimplementation responsibility: Provide light evaluation constants and bounds without reading mutable light components during rendering.
- Source: `Engine/Source/Runtime/Engine/Public/LightSceneProxy.h:44`.

#### FLightSceneInfo

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Renderer light registration and bookkeeping record associated with a light proxy.
- Reimplementation responsibility: Manage light identity, interactions and inclusion in sorted lighting/shadow work.
- Source: `Engine/Source/Runtime/Renderer/Private/LightSceneInfo.h:207`.

#### FSkyLightSceneProxy

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Render-thread sky-light state and environment data reference.
- Reimplementation responsibility: Manage capture results and irradiance/reflection inputs with frame-safe lifetime.
- Source: `Engine/Source/Runtime/Engine/Public/SceneProxies/SkyLightSceneProxy.h:16`.

#### FDeferredDecalProxy

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Render-thread decal representation with material and projection state.
- Reimplementation responsibility: Cull/sort decals and route them into the appropriate depth/material/lighting stages.
- Source: `Engine/Source/Runtime/Engine/Public/SceneProxies/DeferredDecalProxy.h:15`.

#### ISceneExtension

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Extension point for persistent scene systems beyond the monolithic scene record.
- Reimplementation responsibility: Give systems such as shadow caches explicit create/update/destroy integration instead of hiding ownership in frame passes.
- Source: `Engine/Source/Runtime/Renderer/Private/SceneExtensions.h:23`.

### Views, visibility & submission

Per-view state turns persistent scene data into eligible draws and GPU culling work.

#### FSceneViewFamily

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Groups views rendered together with shared show flags, output and family-level settings.
- Reimplementation responsibility: Choose a rendering path and coordinate stereo/multiview output and feature policy.
- Source: `Engine/Source/Runtime/Engine/Public/SceneView.h:2320`.

#### FSceneView

- Kind: class; module: Engine.
- Declared bases: `FSceneViewFixLayout`.
- Role: Public per-view description including transforms, rect and rendering settings.
- Reimplementation responsibility: Compute consistent current/previous view matrices, clipping conventions and screen-to-world reconstruction.
- Source: `Engine/Source/Runtime/Engine/Public/SceneView.h:1481`.

#### FViewInfo

- Kind: class; module: Renderer.
- Declared bases: `FSceneView`.
- Role: Renderer-specific extension of FSceneView carrying visibility results, uniform parameters and per-frame feature data.
- Reimplementation responsibility: Allocate frame-local view resources and connect persistent view history only when it is valid.
- Source: `Engine/Source/Runtime/Renderer/Private/SceneRendering.h:1254`.

#### FSceneViewStateInterface

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Public interface to persistent view state that can survive successive frames.
- Reimplementation responsibility: Separate view lifetime and history ownership from an individual frame's FViewInfo.
- Source: `Engine/Source/Runtime/Engine/Public/SceneManagement.h:118`.

#### FSceneViewState

- Kind: class; module: Renderer.
- Declared bases: `FSceneViewStateInterface`, `FRenderResource`.
- Role: Renderer implementation of persistent view state; also a render resource. Holds histories used by occlusion, Lumen and temporal reconstruction.
- Reimplementation responsibility: Invalidate/reallocate history on camera cuts, view-state changes or incompatible configuration; extract RDG results for later frames.
- Source: `Engine/Source/Runtime/Renderer/Private/SceneViewState.h:59`.

#### FSceneRenderer

- Kind: class; module: Renderer.
- Declared bases: `FSceneRendererBase`.
- Role: Base renderer coordinating scene/view-family rendering, common resources and feature work.
- Reimplementation responsibility: Construct the frame graph from a stable scene snapshot and chosen per-view pipeline state.
- Source: `Engine/Source/Runtime/Renderer/Private/SceneRendering.h:2218`.

#### FDeferredShadingSceneRenderer

- Kind: class; module: Renderer.
- Declared bases: `FSceneRenderer`.
- Role: Desktop deferred renderer that orchestrates visibility, depth, Nanite, material evaluation, shadows, lighting and post processing.
- Reimplementation responsibility: Implement resource dependencies and conditional branches rather than assuming its C++ call order is a serial GPU timeline.
- Source: `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.h:260`.

#### FMobileSceneRenderer

- Kind: class; module: Renderer.
- Declared bases: `FSceneRenderer`.
- Role: Renderer implementation for the mobile feature/platform path.
- Reimplementation responsibility: Treat it as a different rendering path, not a child stage of the desktop deferred pipeline shown here.
- Source: `Engine/Source/Runtime/Renderer/Private/SceneRendering.h:2957`.

#### FMeshBatch

- Kind: struct; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Pass-independent mesh description supplied by a proxy: material proxy, vertex factory, primitive settings and one or more elements.
- Reimplementation responsibility: Keep it as an input description; compile it through a mesh pass processor before command submission.
- Source: `Engine/Source/Runtime/Engine/Public/MeshBatch.h:364`.

#### FMeshBatchElement

- Kind: struct; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: One draw element's geometry range, instance data and related draw parameters within a mesh batch.
- Reimplementation responsibility: Resolve index/vertex ranges and primitive/instance bindings for the chosen pass.
- Source: `Engine/Source/Runtime/Engine/Public/MeshBatch.h:231`.

#### FMeshPassProcessor

- Kind: class; module: Renderer.
- Declared bases: `IPSOCollector`.
- Role: Converts eligible mesh batches into pass-specific draw commands and selects material shaders/render state.
- Reimplementation responsibility: Filter by pass relevance and material policy, resolve shader permutations and emit compatible pipeline/binding commands.
- Source: `Engine/Source/Runtime/Renderer/Public/MeshPassProcessor.h:2258`.

#### FMeshDrawCommand

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Prepared draw state containing shader bindings, pipeline-related state and draw arguments for submission/caching.
- Reimplementation responsibility: Cache only stable state, match dynamic-instancing compatibility and retain referenced resources through GPU completion.
- Source: `Engine/Source/Runtime/Renderer/Public/MeshPassProcessor.h:1281`.

#### FVisibleMeshDrawCommand

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Per-view/pass wrapper selecting a prepared draw command and its visibility/sort information.
- Reimplementation responsibility: Build sorted visible command lists without rebuilding every cached mesh command.
- Source: `Engine/Source/Runtime/Renderer/Public/MeshPassProcessor.h:1762`.

#### FParallelMeshDrawCommandPass

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Coordinates setup and parallel submission of mesh draw commands for a pass.
- Reimplementation responsibility: Join command-setup tasks, perform instance-culling setup and record work into compatible RHI command lists.
- Source: `Engine/Source/Runtime/Renderer/Private/MeshDrawCommands.h:113`.

#### FInstanceCullingManager

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Frame-level coordination for instance-culling resources and contexts across passes/views.
- Reimplementation responsibility: Share scene/view input tables and dispatch GPU work that compacts eligible instances and prepares indirect draws.
- Source: `Engine/Source/Runtime/Renderer/Private/InstanceCulling/InstanceCullingManager.h:44`.

#### FInstanceCullingContext

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Per-pass instance-culling configuration and draw-command work description.
- Reimplementation responsibility: Produce instance-ID streams and indirect argument buffers; declare compute-to-indirect-read dependencies.
- Source: `Engine/Source/Runtime/Renderer/Public/InstanceCulling/InstanceCullingContext.h:75`.

### GPU Scene & instance data

Shader-readable scene tables, stable identifiers, instance transforms and upload machinery.

#### FGPUScene

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: CPU manager for GPU-resident primitive, instance, payload and light-related scene buffers and their updates.
- Reimplementation responsibility: Allocate IDs/ranges, upload dirty data, retire removed entries and expose the right buffers to culling/material/shadow shaders.
- Source: `Engine/Source/Runtime/Renderer/Private/GPUScene.h:218`.

#### FPrimitiveSceneShaderData

- Kind: struct; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Packed shader-facing representation of one primitive's rendering data.
- Reimplementation responsibility: Define matching C++/shader layout for transforms, bounds and primitive properties; never upload the FPrimitiveSceneInfo object itself.
- Source: `Engine/Source/Runtime/Engine/Public/PrimitiveSceneShaderData.h:14`.

#### FInstanceSceneShaderData

- Kind: struct; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Shader-facing per-instance representation used with primitive-level data.
- Reimplementation responsibility: Encode instance transforms/identifiers according to the shader ABI; distinguish instance records from primitive records.
- Source: `Engine/Source/Runtime/Engine/Public/InstanceUniformShaderParameters.h:69`.

#### FInstanceSceneDataBuffers

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: CPU-side instance-data buffers used by instance scene proxies/update paths.
- Reimplementation responsibility: Manage transforms, bounds and optional instance payload consistently before upload to GPU Scene.
- Source: `Engine/Source/Runtime/Engine/Public/InstanceDataSceneProxy.h:134`.

#### FGPUSceneResourceParameters

- Kind: shader parameters; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Shader parameter structure exposing GPU Scene buffers and related access data.
- Reimplementation responsibility: Bind declared SRVs/UAVs through the graph/scene uniform system, not through hidden global raw pointers.
- Source: `Engine/Source/Runtime/Renderer/Private/GPUScene.h:56`.

#### FSceneUniformBuffer

- Kind: class; module: Renderer.
- Declared bases: `TExtensibleUniformBuffer<FSceneUniformParameters, TSceneUniformBufferMemberRegistration>`.
- Role: Extensible scene uniform-buffer wrapper that composes registered scene data for shaders.
- Reimplementation responsibility: Register feature members, update their values and publish a consistent scene uniform buffer for frame consumers.
- Source: `Engine/Source/Runtime/Renderer/Public/SceneUniformBuffer.h:29`.

### Materials, meshes & render resources

Compiled material permutations, vertex factories and render-thread resources bridge assets to RHI objects.

#### FMaterialRenderProxy

- Kind: class; module: Engine.
- Declared bases: `FRenderResource`, `FNoncopyable`.
- Role: Render-thread material interface used by draws to resolve material resources and uniform expressions.
- Reimplementation responsibility: Keep dynamic parameter values separate from shader-map identity and retain fallback materials for unavailable permutations.
- Source: `Engine/Source/Runtime/Engine/Public/Materials/MaterialRenderProxy.h:103`.

#### FMaterial

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Rendering-facing material description used to query shader maps, properties and compilation state.
- Reimplementation responsibility: Expose shading properties and retrieve compatible compiled permutations for the target platform and feature level.
- Source: `Engine/Source/Runtime/Engine/Public/MaterialShared.h:2169`.

#### FMaterialResource

- Kind: class; module: Engine.
- Declared bases: `FMaterial`.
- Role: Material rendering resource that connects an authored material to compiled shader-map data.
- Reimplementation responsibility: Manage shader-map lifetime and provide the rendering material state consumed by mesh processors.
- Source: `Engine/Source/Runtime/Engine/Public/MaterialShared.h:3211`.

#### FMaterialShaderMap

- Kind: class; module: Engine.
- Declared bases: `TShaderMap<FMaterialShaderMapContent, FShaderMapPointerTable>`, `FDeferredCleanupInterface`.
- Role: Collection of compiled shader permutations associated with a material and its rendering configuration.
- Reimplementation responsibility: Cache by material/platform/permutation identity, including vertex-factory/pass variants; avoid recompiling per instance.
- Source: `Engine/Source/Runtime/Engine/Public/MaterialShared.h:1630`.

#### FShader

- Kind: class; module: RenderCore.
- Declared bases: none in the indexed declaration.
- Role: Base compiled shader wrapper and metadata used to access shader code/bindings.
- Reimplementation responsibility: Keep compiled code, reflection/parameter metadata and target-specific RHI shader objects consistent.
- Source: `Engine/Source/Runtime/RenderCore/Public/Shader.h:816`.

#### FGlobalShader

- Kind: class; module: RenderCore.
- Declared bases: `FShader`.
- Role: Shader type for work independent of an individual material, common in compute/full-screen renderer passes.
- Reimplementation responsibility: Compile feature/permutation variants and bind explicit graph parameters for the selected shader.
- Source: `Engine/Source/Runtime/RenderCore/Public/GlobalShader.h:282`.

#### FMaterialShader

- Kind: class; module: Renderer.
- Declared bases: `FShader`.
- Role: Shader base whose compilation/bindings depend on material state.
- Reimplementation responsibility: Combine material uniform data with pass-specific inputs and choose a valid material permutation.
- Source: `Engine/Source/Runtime/Renderer/Public/MaterialShader.h:55`.

#### FMeshMaterialShader

- Kind: class; module: Renderer.
- Declared bases: `FMaterialShader`.
- Role: Material shader specialization that also depends on mesh/vertex-factory data.
- Reimplementation responsibility: Match the material shader to the vertex factory and mesh pass; handle primitive and instance inputs correctly.
- Source: `Engine/Source/Runtime/Renderer/Public/MeshMaterialShader.h:67`.

#### FVertexFactory

- Kind: class; module: RenderCore.
- Declared bases: `FRenderResource`.
- Role: Abstraction describing how a mesh's vertex data is fetched by shader permutations.
- Reimplementation responsibility: Define vertex declarations/streams or manual-fetch interfaces; it is not itself the geometry asset or a GPU Scene table.
- Source: `Engine/Source/Runtime/RenderCore/Public/VertexFactory.h:655`.

#### FLocalVertexFactory

- Kind: class; module: Engine.
- Declared bases: `FVertexFactory`.
- Role: Vertex factory for conventional local/static mesh vertex streams.
- Reimplementation responsibility: Bind position/tangent/UV/color streams and their material-shader interface.
- Source: `Engine/Source/Runtime/Engine/Public/LocalVertexFactory.h:75`.

#### FRenderResource

- Kind: class; module: RenderCore.
- Declared bases: none in the indexed declaration.
- Role: Render-thread-managed resource lifecycle abstraction. It can own several RHI objects or no single GPU allocation.
- Reimplementation responsibility: Implement initialization/release ordering and retain dependencies; distinguish render resource lifetime from UObject and FRHIResource lifetimes.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderResource.h:36`.

#### FVertexBuffer

- Kind: class; module: RenderCore.
- Declared bases: `FRenderResource`.
- Role: Render resource wrapper for a vertex buffer RHI allocation.
- Reimplementation responsibility: Upload/create the buffer and expose vertex-fetch-compatible bindings.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderResource.h:377`.

#### FIndexBuffer

- Kind: class; module: RenderCore.
- Declared bases: `FRenderResource`.
- Role: Render resource wrapper for index-buffer storage.
- Reimplementation responsibility: Track element width and topology/range constraints when preparing indexed draws.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderResource.h:412`.

#### FTextureResource

- Kind: class; module: Engine.
- Declared bases: `FTexture`.
- Role: Render-resource representation of a texture asset with RHI texture/sampler state.
- Reimplementation responsibility: Upload/stream mips and maintain valid texture views and sampler bindings.
- Source: `Engine/Source/Runtime/Engine/Public/TextureResource.h:102`.

#### FStaticMeshRenderData

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Runtime static-mesh render data, including LOD resources and optional feature representations.
- Reimplementation responsibility: Build/cook shared render buffers and arrange feature-specific data without conflating asset geometry with per-instance transforms.
- Source: `Engine/Source/Runtime/Engine/Public/StaticMeshResources.h:771`.

#### FStaticMeshLODResources

- Kind: struct; module: Engine.
- Declared bases: `UE::Private::FQueryableRefCountedObject`.
- Role: Geometry buffers and sections for one conventional static-mesh LOD.
- Reimplementation responsibility: Maintain section/material mapping, index ranges and vertex-factory input buffers.
- Source: `Engine/Source/Runtime/Engine/Public/StaticMeshResources.h:417`.

#### FSkeletalMeshRenderData

- Kind: class; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Shared rendering data for skeletal-mesh LODs.
- Reimplementation responsibility: Provide immutable LOD streams while per-object animation supplies changing deformation inputs.
- Source: `Engine/Source/Runtime/Engine/Public/Rendering/SkeletalMeshRenderData.h:16`.

#### FSkeletalMeshLODRenderData

- Kind: class; module: Engine.
- Declared bases: `UE::Private::FQueryableRefCountedObject`.
- Role: One skeletal mesh render LOD's vertex/index/section data.
- Reimplementation responsibility: Support bone influences and render sections with compatible skinning vertex factories and ray geometry updates.
- Source: `Engine/Source/Runtime/Engine/Public/Rendering/SkeletalMeshLODRenderData.h:136`.

### Nanite representation

Cluster/page geometry, streaming, packed views, visibility rasterization and material shading commands.

#### Nanite::FResources

- Kind: struct; module: Engine.
- Declared bases: none in the indexed declaration.
- Role: Cooked/runtime Nanite asset representation containing page/cluster hierarchy and streaming metadata.
- Reimplementation responsibility: Build a hierarchical cluster representation offline, package resident/streamed pages and expose root resources at runtime.
- Source: `Engine/Source/Runtime/Engine/Public/Rendering/NaniteResources.h:452`.

#### Nanite::FStreamingManager

- Kind: class; module: Engine.
- Declared bases: `FRenderResource`.
- Role: Render resource managing Nanite page residency, requests and streaming updates.
- Reimplementation responsibility: Process feedback, prioritize requests, upload pages, apply fixups and keep hierarchy traversal safe when detail pages are missing.
- Source: `Engine/Source/Runtime/Engine/Public/Rendering/NaniteStreamingManager.h:68`.

#### Nanite::FPackedView

- Kind: struct; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: GPU-oriented view record for Nanite culling/rasterization, including transforms, bounds and LOD/culling parameters.
- Reimplementation responsibility: Pack camera/shadow/capture views consistently with shader layouts and the target raster extent.
- Source: `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteShared.h:59`.

#### Nanite::FRasterContext

- Kind: struct; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Nanite raster target/configuration context used to create visibility/depth output.
- Reimplementation responsibility: Allocate/clear compatible raster resources and select supported hardware/software raster paths.
- Source: `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteCullRaster.h:56`.

#### Nanite::FRasterResults

- Kind: struct; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Outputs of Nanite visibility rasterization, including resources needed by depth export, material shading and visualization.
- Reimplementation responsibility: Carry visibility/cluster/bin information into later passes; a visibility buffer is not a finished GBuffer or lit scene color.
- Source: `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteCullRaster.h:77`.

#### FNaniteShadingCommands

- Kind: struct; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Prepared material-shading commands for Nanite mesh passes.
- Reimplementation responsibility: Build shading bins/work from visible material assignments, then evaluate material attributes into the scene's shading representation.
- Source: `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteShared.h:1117`.

#### FNaniteRasterPipelines

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Registry of Nanite raster pipeline variants used by mesh passes.
- Reimplementation responsibility: Separate raster material needs such as masks/deformation from later material shading and cache compatible pipeline variants.
- Source: `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteShared.h:806`.

#### FNaniteShadingPipelines

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Registry of Nanite shading pipeline/material variants.
- Reimplementation responsibility: Associate visible material work with compiled shading pipelines and dispatch state.
- Source: `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteShared.h:1043`.

### Lumen representation

Cards, surface-cache pages, cached lighting, screen probes and persistent radiance/history state.

#### FLumenSceneData

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Persistent Lumen scene representation: primitive groups, mesh cards, surface-cache allocation and lighting-related data.
- Reimplementation responsibility: Track scene changes and residency, budget captures/lighting updates and retain feature state across frames.
- Source: `Engine/Source/Runtime/Renderer/Private/Lumen/LumenSceneData.h:1094`.

#### FLumenPrimitiveGroup

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Grouping of primitives/instances used to manage Lumen representation and surface-cache work.
- Reimplementation responsibility: Choose capture/representation granularity, track bounds and invalidate affected cards when geometry changes.
- Source: `Engine/Source/Runtime/Renderer/Private/Lumen/LumenSceneData.h:433`.

#### FLumenMeshCards

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Mesh-level collection/placement of cards used to parameterize surfaces for Lumen.
- Reimplementation responsibility: Transform asset card descriptions to the scene and connect them to primitive groups and card allocation.
- Source: `Engine/Source/Runtime/Renderer/Private/Lumen/LumenMeshCards.h:28`.

#### FLumenCard

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Oriented surface parameterization used to capture/material-cache a portion of geometry.
- Reimplementation responsibility: Track card transforms, extents and allocated mip/page state; cards approximate surface coverage, not a second triangle mesh for primary visibility.
- Source: `Engine/Source/Runtime/Renderer/Private/Lumen/LumenSceneData.h:307`.

#### FLumenSurfaceCacheAllocator

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Allocates physical atlas space for Lumen surface-cache pages.
- Reimplementation responsibility: Handle page residency, reuse and eviction; prevent stale mappings and budget memory/capture work.
- Source: `Engine/Source/Runtime/Renderer/Private/Lumen/LumenSceneData.h:636`.

#### FScreenProbeGatherParameters

- Kind: shader parameters; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Shader parameter bundle for Lumen screen-probe placement, tracing and gather work.
- Reimplementation responsibility: Bind screen depth/normals, probe atlases, trace results and integration data explicitly through RDG.
- Source: `Engine/Source/Runtime/Renderer/Private/Lumen/LumenScreenProbeGather.h:154`.

#### FRadianceCacheState

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Persistent world-space radiance-cache state associated with Lumen view history.
- Reimplementation responsibility: Manage probe allocation, clipmap placement, update budgets and temporal reuse. This is distinct from the surface cache of material/lighting on cards.
- Source: `Engine/Source/Runtime/Renderer/Private/Lumen/LumenViewState.h:234`.

### Shadows & ray representations

VSM depth pages, mesh/global distance fields, BLAS/TLAS and ray tracing commands are distinct scene representations.

#### FVirtualShadowMapArray

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Frame-level virtual shadow map resources and rendering operations for lights.
- Reimplementation responsibility: Mark requested virtual pages, allocate/cache physical depth pages, rasterize missing/invalid pages and project shadows onto receivers.
- Source: `Engine/Source/Runtime/Renderer/Private/VirtualShadowMaps/VirtualShadowMapArray.h:293`.

#### FVirtualShadowMapArrayCacheManager

- Kind: class; module: Renderer.
- Declared bases: `ISceneExtension`.
- Role: Persistent scene extension managing VSM cache state across frames.
- Reimplementation responsibility: Track light/primitive changes and invalidate affected pages; retain extracted resources and reuse only valid depth pages.
- Source: `Engine/Source/Runtime/Renderer/Private/VirtualShadowMaps/VirtualShadowMapCacheManager.h:242`.

#### FVirtualShadowMapClipmap

- Kind: class; module: Renderer.
- Declared bases: `FRefCountedObject`.
- Role: Directional-light clipmap organization providing virtual shadow coverage around views.
- Reimplementation responsibility: Map world-space coverage across levels and update mappings as the camera/light changes.
- Source: `Engine/Source/Runtime/Renderer/Private/VirtualShadowMaps/VirtualShadowMapClipmap.h:42`.

#### FProjectedShadowInfo

- Kind: class; module: Renderer.
- Declared bases: `FRefCountedObject`.
- Role: Renderer description/state for a projected shadow and its subject/receiver work.
- Reimplementation responsibility: Build shadow views, collect casters and route conventional or applicable virtual-shadow work.
- Source: `Engine/Source/Runtime/Renderer/Private/ShadowRendering.h:336`.

#### FDistanceFieldSceneData

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Renderer scene bookkeeping for mesh distance-field representations and their GPU data.
- Reimplementation responsibility: Upload object bounds/transforms and compose/update the global distance field when the selected features require it.
- Source: `Engine/Source/Runtime/Renderer/Private/ScenePrivate.h:834`.

#### FRayTracingGeometry

- Kind: class; module: RenderCore.
- Declared bases: `FRenderResource`.
- Role: Render-resource wrapper for ray-tracing geometry, normally backed by a bottom-level acceleration structure.
- Reimplementation responsibility: Create/build/update BLAS from geometry streams and synchronize deformation/upload before tracing consumers.
- Source: `Engine/Source/Runtime/RenderCore/Public/RayTracingGeometry.h:28`.

#### FRayTracingScene

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Renderer coordinator for ray-tracing instances and top-level acceleration-structure state.
- Reimplementation responsibility: Collect visible/eligible instances, upload instance descriptors, build TLAS and retain geometry references; it is separate from FScene and GPU Scene.
- Source: `Engine/Source/Runtime/Renderer/Private/RayTracing/RayTracingScene.h:46`.

#### FRayTracingInstance

- Kind: struct; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Ray-tracing instance description connecting geometry, transforms, masks and material bindings.
- Reimplementation responsibility: Choose the geometry representation for each instance and preserve hit-group/material mapping.
- Source: `Engine/Source/Runtime/Renderer/Public/RayTracingInstance.h:24`.

#### FRayTracingMeshCommand

- Kind: class; module: Renderer.
- Declared bases: none in the indexed declaration.
- Role: Prepared material/shader binding information for ray-tracing mesh sections.
- Reimplementation responsibility: Build consistent hit-shader or material-binding data alongside the acceleration structures.
- Source: `Engine/Source/Runtime/Renderer/Public/RayTracingMeshDrawCommands.h:69`.

### Render Dependency Graph

Logical frame resources, pass dependencies, lifetime tracking and extraction into persistent storage.

#### FRDGBuilder

- Kind: class; module: RenderCore.
- Declared bases: `FRDGScopeState`.
- Role: Builds and executes a Render Dependency Graph from passes and declared resource accesses.
- Reimplementation responsibility: Track reads/writes, cull unused work, plan lifetimes/barriers, schedule supported async work and execute through RHI command lists.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphBuilder.h:50`.

#### FRDGPass

- Kind: class; module: RenderCore.
- Declared bases: none in the indexed declaration.
- Role: Internal graph pass representation with parameters, dependencies and execution work.
- Reimplementation responsibility: Represent pass resource accesses and execution flags; graph order is constrained by dependencies and observable outputs.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphPass.h:217`.

#### FRDGResource

- Kind: class; module: RenderCore.
- Declared bases: none in the indexed declaration.
- Role: Base graph resource bookkeeping and identity.
- Reimplementation responsibility: Track lifetime and accesses within a graph; a graph handle is not a general persistent GPU ownership token.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:134`.

#### FRDGViewableResource

- Kind: class; module: RenderCore.
- Declared bases: `FRDGResource`.
- Role: RDG resource base for buffers/textures supporting views and access tracking.
- Reimplementation responsibility: Connect graph identity to allocated or imported RHI storage and per-view usage.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:293`.

#### FRDGTexture

- Kind: class; module: RenderCore.
- Declared bases: `FRDGViewableResource`.
- Role: Logical texture in an RDG graph, transient or imported from persistent storage.
- Reimplementation responsibility: Describe format/extent/usage and declare subresource accesses; extract/import explicitly when results must survive a frame.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:569`.

#### FRDGBuffer

- Kind: class; module: RenderCore.
- Declared bases: `FRDGViewableResource`.
- Role: Logical buffer in an RDG graph.
- Reimplementation responsibility: Declare structured/raw/indirect use and compute/graphics access transitions; choose transient or persistent storage intentionally.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:1307`.

#### FRDGTextureSRV

- Kind: class; module: RenderCore.
- Declared bases: `FRDGShaderResourceView`.
- Role: Shader-resource view of an RDG texture.
- Reimplementation responsibility: Expose read access to the intended mip/slice/format while retaining graph dependency tracking.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:837`.

#### FRDGTextureUAV

- Kind: class; module: RenderCore.
- Declared bases: `FRDGUnorderedAccessView`.
- Role: Unordered-access view of an RDG texture.
- Reimplementation responsibility: Declare shader write/read-write accesses and required ordering before later sampling or attachment use.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:903`.

#### FRDGBufferSRV

- Kind: class; module: RenderCore.
- Declared bases: `FRDGShaderResourceView`.
- Role: Read view of an RDG buffer.
- Reimplementation responsibility: Match buffer format/stride and declare shader reads in pass parameters.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:1417`.

#### FRDGBufferUAV

- Kind: class; module: RenderCore.
- Declared bases: `FRDGUnorderedAccessView`.
- Role: Unordered-access view of an RDG buffer.
- Reimplementation responsibility: Track compute writes/atomics and transitions to draw-indirect or shader-read consumers.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:1443`.

#### FRDGUniformBuffer

- Kind: class; module: RenderCore.
- Declared bases: `FRDGResource`.
- Role: Graph-managed uniform-buffer object whose contents may reference other graph resources.
- Reimplementation responsibility: Preserve nested resource dependencies when binding structured pass/scene parameters.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:195`.

#### FRDGPooledBuffer

- Kind: class; module: RenderCore.
- Declared bases: `UE::Private::FQueryableRefCountedObject`.
- Role: Reference-counted backing buffer that can persist outside one graph.
- Reimplementation responsibility: Retain extracted buffers and register them with a later graph; distinguish backing lifetime from FRDGBuffer handle lifetime.
- Source: `Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h:1194`.

#### IPooledRenderTarget

- Kind: struct; module: RenderCore.
- Declared bases: none in the indexed declaration.
- Role: Interface to pooled texture storage that can outlive frame-local graph handles.
- Reimplementation responsibility: Retain persistent history/output textures, manage descriptors and import them explicitly into RDG.
- Source: `Engine/Source/Runtime/RenderCore/Public/PooledRenderTarget.h:433`.

#### FSceneTextures

- Kind: struct; module: Renderer.
- Declared bases: `FMinimalSceneTextures`.
- Role: Bundle of the active scene's depth, color, GBuffer and other view-family texture resources.
- Reimplementation responsibility: Allocate compatible scene textures and refresh shader parameter bindings as depth, velocity and lighting outputs become available.
- Source: `Engine/Source/Runtime/Renderer/Internal/SceneTextures.h:111`.

### RHI & native GPU objects

Portable resource/command contracts and platform implementations. These C++ wrappers are usually CPU objects that own or reference GPU state.

#### FRHIResource

- Kind: class; module: RHI.
- Declared bases: none in the indexed declaration.
- Role: Reference-counted base for portable RHI resource objects.
- Reimplementation responsibility: Implement resource lifetime and destruction after GPU use; CPU reference lifetime alone is not a substitute for submission retention.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:53`.

#### FRHIViewableResource

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: RHI base for storage that can be referenced by shader/resource views.
- Reimplementation responsibility: Track storage identity and access requirements separately from view descriptors.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:1314`.

#### FRHIBuffer

- Kind: class; module: RHI.
- Declared bases: `FRHIViewableResource`, `RHIValidation::FBufferResource`.
- Role: Portable buffer allocation contract.
- Reimplementation responsibility: Create vertex/index/structured/raw/indirect-capable storage with valid size, usage and access transitions.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:1627`.

Conditional base `RHIValidation::FBufferResource`: #if ENABLE_RHI_VALIDATION. This mixin is not enabled in every build.

#### FRHITexture

- Kind: class; module: RHI.
- Declared bases: `FRHIViewableResource`, `RHIValidation::FTextureResource`.
- Role: Portable texture allocation contract.
- Reimplementation responsibility: Describe dimensions, format, mip/array/sample layout and support the views/transitions needed by renderer passes.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:2167`.

Conditional base `RHIValidation::FTextureResource`: #if ENABLE_RHI_VALIDATION. This mixin is not enabled in every build.

#### FRHIUniformBuffer

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`, `RHIValidation::FUniformBufferResource`.
- Role: RHI uniform/constant-buffer object with a declared layout and resource bindings.
- Reimplementation responsibility: Keep CPU/shader layout and bound resource lifetimes consistent.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:1262`.

Conditional base `RHIValidation::FUniformBufferResource`: #if ENABLE_RHI_VALIDATION. This mixin is not enabled in every build.

#### FRHIShaderResourceView

- Kind: class; module: RHI.
- Declared bases: `FRHIView`.
- Role: Portable shader read view of compatible storage.
- Reimplementation responsibility: Create typed/raw/structured or texture subresource views and bind them to shaders.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:3369`.

#### FRHIUnorderedAccessView

- Kind: class; module: RHI.
- Declared bases: `FRHIView`.
- Role: Portable shader unordered-access view.
- Reimplementation responsibility: Support storage writes/atomics and order them against subsequent uses.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:3359`.

#### FRHIShader

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`, `FRHIShaderData`.
- Role: Base RHI shader object for compiled platform shader code.
- Reimplementation responsibility: Create native shader representations and match parameter/resource bindings to compiled code.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:842`.

#### FRHIVertexShader

- Kind: class; module: RHI.
- Declared bases: `FRHIGraphicsShader`.
- Role: RHI vertex-stage shader object.
- Reimplementation responsibility: Consume vertex-factory data and produce rasterization inputs with the correct pipeline interface.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:987`.

#### FRHIPixelShader

- Kind: class; module: RHI.
- Declared bases: `FRHIGraphicsShader`.
- Role: RHI fragment/pixel-stage shader object.
- Reimplementation responsibility: Evaluate pass/material outputs and match attachment formats and render state.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:1005`.

#### FRHIComputeShader

- Kind: class; module: RHI.
- Declared bases: `FRHIShader`.
- Role: RHI compute-stage shader object.
- Reimplementation responsibility: Bind compute resources and launch direct/indirect thread groups with explicit dependencies.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:1051`.

#### FRHIGraphicsPipelineState

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: Prepared graphics pipeline state object.
- Reimplementation responsibility: Combine shaders, vertex declarations, fixed-function state and attachment compatibility; cache by all relevant state.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:1091`.

#### FRHIComputePipelineState

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: Prepared compute pipeline state object.
- Reimplementation responsibility: Create/cache the native compute pipeline associated with shader and binding requirements.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:1111`.

#### FRHIRayTracingGeometry

- Kind: class; module: RHI.
- Declared bases: `FRHIRayTracingAccelerationStructure`.
- Role: Portable bottom-level ray-tracing acceleration-structure resource contract.
- Reimplementation responsibility: Translate geometry descriptions into native BLAS storage/build commands and barriers.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:3768`.

#### FRHIRayTracingScene

- Kind: class; module: RHI.
- Declared bases: `FRHIRayTracingAccelerationStructure`.
- Role: Portable top-level ray-tracing acceleration-structure resource contract.
- Reimplementation responsibility: Build native TLAS from instance descriptions and expose it to supported tracing shader/inline-query paths.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:3816`.

#### FRHIRayTracingPipelineState

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`, `RHIValidation::FRayTracingPipelineState`.
- Role: RHI pipeline object for ray-generation/miss/hit shader execution.
- Reimplementation responsibility: Create shader collections/pipelines and compatible binding tables when using pipeline-based ray tracing.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:1145`.

Conditional base `RHIValidation::FRayTracingPipelineState`: #if ENABLE_RHI_VALIDATION. This mixin is not enabled in every build.

#### FRHISamplerState

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: Portable texture sampling state.
- Reimplementation responsibility: Specify filtering, addressing and related sampling policy separately from texture allocation/view identity.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:633`.

#### FRHIRasterizerState

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: Portable rasterization state.
- Reimplementation responsibility: Set culling, fill and depth-bias behavior consistent with the target pass.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:641`.

#### FRHIDepthStencilState

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: Portable depth/stencil test and write state.
- Reimplementation responsibility: Specify comparison, write masks and stencil operations; keep resource states and attachment access compatible.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:648`.

#### FRHIBlendState

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: Portable color blending/write-mask state.
- Reimplementation responsibility: Choose blend equations and masks per render target, especially for lighting and translucency composition.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:658`.

#### FRHIVertexDeclaration

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: Portable vertex-input element/stream layout.
- Reimplementation responsibility: Match attribute formats, offsets and stream slots to the vertex factory and shader input.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:687`.

#### FRHIGPUFence

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: GPU completion marker queried/waited from appropriate CPU workflows.
- Reimplementation responsibility: Use for readback/recycling and completion tracking; resource barriers solve a different ordering problem.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:2397`.

#### FRHIViewport

- Kind: class; module: RHI.
- Declared bases: `FRHIResource`.
- Role: RHI presentation target interface associated with a viewport/swap chain.
- Reimplementation responsibility: Manage back-buffer acquisition, resize and presentation outside the deferred lighting algorithm itself.
- Source: `Engine/Source/Runtime/RHI/Public/RHIResources.h:2544`.

#### FRHICommandListBase

- Kind: class; module: RHI.
- Declared bases: none in the indexed declaration.
- Role: Base command-list recording and execution infrastructure.
- Reimplementation responsibility: Record resource/command work with thread-safe ownership and proper submission lifetime.
- Source: `Engine/Source/Runtime/RHI/Public/RHICommandList.h:453`.

#### FRHIComputeCommandList

- Kind: class; module: RHI.
- Declared bases: `FRHICommandListBase`.
- Role: Command-list interface for compute-capable operations.
- Reimplementation responsibility: Record dispatches, transitions and compute-compatible commands; availability of a separate hardware queue is a platform decision.
- Source: `Engine/Source/Runtime/RHI/Public/RHICommandList.h:2493`.

#### FRHICommandList

- Kind: class; module: RHI.
- Declared bases: `FRHIComputeCommandList`.
- Role: Graphics-capable command list extending compute command functionality.
- Reimplementation responsibility: Record render passes/draws and synchronize them with compute/copy work.
- Source: `Engine/Source/Runtime/RHI/Public/RHICommandList.h:3590`.

#### FRHICommandListImmediate

- Kind: class; module: RHI.
- Declared bases: `FRHICommandList`.
- Role: Immediate command-list specialization coordinating renderer/RHI execution.
- Reimplementation responsibility: Flush/submit at defined boundaries; immediate API naming does not mean each call has completed on the GPU.
- Source: `Engine/Source/Runtime/RHI/Public/RHICommandList.h:4388`.

#### FDynamicRHI

- Kind: class; module: RHI.
- Declared bases: none in the indexed declaration.
- Role: Platform RHI implementation interface and resource/device entry points.
- Reimplementation responsibility: Translate portable resource and capability contracts to the selected graphics backend.
- Source: `Engine/Source/Runtime/RHI/Public/DynamicRHI.h:198`.

#### IRHICommandContext

- Kind: class; module: RHI.
- Declared bases: `IRHIComputeContext`.
- Role: Platform command-context interface implementing graphics/compute recording operations.
- Reimplementation responsibility: Emit native commands and maintain native command state behind the portable RHI interface.
- Source: `Engine/Source/Runtime/RHI/Public/RHIContext.h:686`.

#### FD3D12DynamicRHI

- Kind: class; module: D3D12RHI.
- Declared bases: `ID3D12PlatformDynamicRHI`.
- Role: Direct3D 12 implementation of the dynamic RHI.
- Reimplementation responsibility: Own D3D12 devices/queues and implement portable RHI creation/submission contracts.
- Source: `Engine/Source/Runtime/D3D12RHI/Private/D3D12RHIPrivate.h:158`.

#### FVulkanDynamicRHI

- Kind: class; module: VulkanRHI.
- Declared bases: `IVulkanDynamicRHI`.
- Role: Vulkan implementation of the dynamic RHI.
- Reimplementation responsibility: Map RHI operations onto Vulkan devices, queues, synchronization and resources.
- Source: `Engine/Source/Runtime/VulkanRHI/Private/VulkanDynamicRHI.h:66`.

#### FMetalDynamicRHI

- Kind: class; module: MetalRHI.
- Declared bases: `IMetalDynamicRHI`.
- Role: Metal implementation of the dynamic RHI.
- Reimplementation responsibility: Map RHI capabilities/resources/commands to Metal objects and execution constraints.
- Source: `Engine/Source/Runtime/Apple/MetalRHI/Private/MetalDynamicRHI.h:73`.

## Deferred rendering stages

### 01. Scene update & GPU Scene (scene-update)

Turn queued world changes into stable render-thread records and shader-readable primitive/instance tables.

Lane: setup; column: 1; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: frame inputs.

**Inputs**

1. Component/proxy additions, removals and changes
2. Persistent FScene and previous-frame state

**Outputs**

1. Updated primitive/light records and GPU Scene tables
2. Resource lifetime and upload dependencies

**How it works**

1. Snapshot game-thread state into proxies; render passes consume the render-thread representation.
2. Apply scene changes, update bounds and light interactions, and invalidate affected cached mesh commands.
3. Allocate primitive/instance IDs and upload changed shader data. Keep previous transforms for motion vectors.
4. Retain old resources until queued CPU and GPU consumers no longer reference them.

**Operations to implement**

1. Stable handles with generation counters and deferred reclamation
2. Add/update/remove queues plus dirty-range tracking
3. Structured-buffer allocation, scatter uploads and upload barriers
4. Current/previous transform storage and scene-uniform binding

**Correctness checks**

1. Repeatedly add/remove/move instances while frames are in flight; detect stale IDs.
2. Compare shader-read transforms with CPU reference data; check velocity after teleport and ordinary motion.

Related types: FScene, FPrimitiveSceneProxy, FPrimitiveSceneInfo, FGPUScene.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/SceneRendering.cpp:4198` — `IVisibilityTaskData* FSceneRenderer::OnRenderBegin`
- `Engine/Source/Runtime/Renderer/Private/GPUScene.cpp:1782` — `void FGPUScene::Update(`

### 02. Views, relevance & draw setup (views)

Build per-view visibility and submission work while retaining persistent temporal state separately.

Lane: setup; column: 2; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: scene-update.

**Inputs**

1. Scene records, bounds and view family
2. Camera matrices, show flags and feature/platform settings

**Outputs**

1. FViewInfo visibility/relevance
2. Mesh draw commands and instance-culling work
3. Allocated scene textures

**How it works**

1. Initialize current/previous view transforms, jitter, frustum and view-state references.
2. Cull primitives and classify material/pass relevance; select conventional mesh LODs where applicable.
3. Reuse valid cached FMeshDrawCommands or build dynamic commands through mesh-pass processors.
4. Prepare instance culling and pass resource bindings. Nanite has additional GPU hierarchy/cluster visibility later.

**Operations to implement**

1. View/frustum construction and bounds tests
2. Visibility bitsets, relevance classification and LOD selection
3. Material/vertex-factory/pass shader and PSO lookup
4. Draw sorting/batching, indirect arguments and instance culling
5. Depth, GBuffer, scene-color and velocity texture allocation

**Correctness checks**

1. Test two views with different visibility and independent histories.
2. Compare cached and rebuilt draws after material changes; verify hidden primitives do not submit work.

Related types: FSceneViewFamily, FViewInfo, FSceneViewState, FMeshBatch, FMeshDrawCommand, FInstanceCullingManager.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:2123` — `BeginInitViews(`
- `Engine/Source/Runtime/Renderer/Private/SceneVisibility.cpp:5935` — `ComputeViewVisibility`

### 03. Nanite residency & packed views (streaming)

Make virtualized geometry pages and per-view culling parameters available before Nanite work.

Lane: geometry; column: 3; requires: nanite; async-capable grouping: False.

Major prerequisites: views.

**Inputs**

1. Cooked Nanite hierarchy/cluster pages
2. Prior streaming requests and camera/instance data

**Outputs**

1. Resident page mappings and hierarchy buffers
2. Packed views and raster/shading pipeline state

**How it works**

1. Cooked resources describe clustered geometry and a traversable hierarchy; runtime streaming does not build the asset from scratch each frame.
2. Process feedback, upload requested pages and maintain valid resident geometry/coarser fallbacks.
3. Pack view matrices, rectangles and LOD/culling parameters, including previous occlusion data when valid.
4. Resolve programmable raster/material state needed by the visible scene.

**Operations to implement**

1. Offline mesh clustering, hierarchy construction, encoding and error metrics
2. Page pool, virtual-to-physical mappings, request queues and bounded streaming uploads
3. GPU hierarchy/page tables and packed-view buffers
4. Residency-safe refinement and streaming feedback readback

**Correctness checks**

1. Force a tiny page budget and rapid camera travel; verify missing detail degrades without holes.
2. Invalidate old occlusion data after camera cuts and verify newly visible geometry appears.

Related types: Nanite::FResources, Nanite::FStreamingManager, Nanite::FPackedView.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:2048` — `GStreamingManager.BeginAsyncUpdate`
- `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteCullRaster.cpp:6688` — `FRenderer::DrawGeometry`

### 04. Early atmosphere lighting inputs (atmosphere-luts)

Prepare enabled atmosphere lookup resources before lighting systems that consume the sky environment.

Lane: caches; column: 3; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: views.

**Inputs**

1. Atmosphere parameters, lights and view data
2. Cloud-shadow data when used by the selected LUT schedule

**Outputs**

1. Atmosphere lookup resources available to Lumen scene lighting and later sky composition

**How it works**

1. Select the before-prepass or before-base-pass LUT scheduling mode.
2. Declare transmittance/scattering/sky lookup work and any cloud-shadow prerequisite selected by that path.
3. Publish/commit the pending atmosphere resources before Lumen scene consumers need them.
4. Later sky/fog/cloud composition consumes related resources after opaque lighting; this early node does not composite the final sky image.

**Operations to implement**

1. Atmosphere parameter upload and lookup texture allocation
2. Transmittance/scattering lookup generation and selected cloud-shadow inputs
3. Pending-resource publication and graph read/write dependencies

**Correctness checks**

1. Switch the supported LUT scheduling mode and compare lighting results.
2. Change the sun/atmosphere parameters and verify Lumen scene lighting does not use stale lookup state.

Configuration/order caveat: Optional atmosphere work; absent atmosphere features use the appropriate defaults. This column is an early scheduling window, not a claim both alternative LUT calls execute every frame.

Related types: FSceneTextures, FRDGTexture.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:2438` — `RenderSkyAtmosphereLookUpTables(`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:2849` — `RenderSkyAtmosphereLookUpTables(`

### 05. Conventional depth prepass (depth)

Rasterize eligible non-Nanite geometry into scene depth; exact coverage depends on the early-Z mode.

Lane: geometry; column: 4; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: views.

**Inputs**

1. Visible depth-pass mesh commands
2. Scene depth target, material masking and current/previous transforms

**Outputs**

1. Early scene depth/stencil
2. Optional early velocity

**How it works**

1. Clear/configure depth and stencil using the renderer's depth convention.
2. Draw the configured opaque/masked subset with depth-compatible material permutations.
3. Generate velocity here only when the selected mode requires it; other configurations write it in the base pass or a later pass.
4. Make resolved depth available to consumers when required.

**Operations to implement**

1. Depth-only PSOs and alpha-test/WPO-compatible vertex/material evaluation
2. Indirect indexed draws with instance data
3. Depth/stencil transitions, clears and resolves
4. Consistent current/previous clip-space motion-vector encoding

**Correctness checks**

1. Check masked cutouts and displaced geometry agree between depth and base passes.
2. Test early-Z/velocity modes; avoid missing or double-written motion vectors.

Configuration/order caveat: The prepass is configurable. DBuffer, Nanite, shadows and other features can impose depth requirements; this box is not a promise that every primitive writes depth here.

Related types: FMeshPassProcessor, FSceneTextures.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:2464` — `RenderPrePass(GraphBuilder, InViews`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:2100` — `DDM_AllOpaqueNoVelocity`

### 06. Distance fields / ray tracing AS (trace-scene)

Prepare the non-screen geometry representation used by Lumen tracing and other ray-based effects.

Lane: caches; column: 4; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: scene-update, views.

**Inputs**

1. Renderable geometry, transforms, deformation and feature settings
2. Cooked distance fields or ray-tracing geometry buffers

**Outputs**

1. Software: mesh/global distance-field resources
2. Hardware: BLAS/TLAS and instance/material data

**How it works**

1. Software tracing uses mesh distance fields where enabled, then broader global-distance-field coverage for eligible remaining work.
2. Hardware tracing builds/refits bottom-level geometry AS and a top-level instance AS; shader/material data must agree with the geometry representation.
3. Update dynamic geometry and instance transforms, and establish build-to-trace synchronization.
4. Nanite primary visibility and the hardware RT representation can differ. Fallback geometry or separately enabled native Nanite ray tracing affects correspondence.

**Operations to implement**

1. Software path: mesh SDF generation, spatial object lists, GDF clipmaps/composition and updates
2. Hardware path: geometry descriptors, BLAS build/refit, TLAS instances/build and scratch allocation
3. Ray instance/material lookup tables and geometry lifetime tracking
4. Build/upload barriers and representation-specific invalidation

**Correctness checks**

1. Compare raster and traced silhouettes, especially Nanite fallback meshes and deformed objects.
2. Check thin surfaces, moving instances, clipmap transitions and off-screen occluders.

Configuration/order caveat: This node combines alternative representations. Hardware tracing is not required for Lumen; screen tracing does not eliminate the need for a valid fallback scene. AS setup may run later when its first consumer allows it.

Related types: FDistanceFieldSceneData, FRayTracingGeometry, FRayTracingScene, FRHIRayTracingScene.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:2011` — `PrepareDistanceFieldScene(`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3034` — `SetupRayTracingRenderingData(`
- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenScreenProbeTracing.cpp:797` — `RenderHardwareRayTracingScreenProbe`

### 07. Nanite cull → raster → repair (nanite-visibility)

Traverse visible geometry, rasterize cluster/triangle visibility, then recover geometry incorrectly rejected by previous-frame occlusion.

Lane: geometry; column: 5; requires: nanite; async-capable grouping: False.

Major prerequisites: streaming, depth.

**Inputs**

1. Resident Nanite pages, GPU Scene and packed views
2. Previous HZB when valid; current conventional depth

**Outputs**

1. Nanite visibility buffer and depth
2. Visible cluster lists, raster results and streaming requests

**How it works**

1. Cull instances and hierarchy nodes, choose appropriate cluster detail, and queue visible clusters.
2. The main occlusion pass uses the previous HZB when two-pass occlusion is enabled. Rejected candidates remain eligible for a post pass.
3. Raster bins dispatch hardware rasterization and software compute rasterization according to work/material/platform policy; small triangles benefit from the software path.
4. Build a current HZB after main visibility, re-test previously occluded candidates and rasterize recovered geometry in the post pass.
5. Export Nanite depth/stencil and metadata for shared scene consumers. The visibility buffer identifies winning geometry; it is not the final shaded GBuffer.

**Operations to implement**

1. GPU work queues, hierarchy traversal, bounds/frustum/occlusion tests and LOD error selection
2. Work compaction, counters, indirect dispatch/draw arguments and overflow handling
3. Hardware and compute raster paths with a consistent depth/visibility winner rule
4. HZB reduction, main/post candidate lists and recovery rasterization
5. Depth export, visibility decoding and material/raster-bin metadata

**Correctness checks**

1. Move a large occluder away and rotate the camera: newly exposed clusters must recover in the same frame.
2. Compare hardware/software raster overlap and edge depth; test masked/WPO bins and queue overflow.

Configuration/order caveat: Two-pass occlusion is conditional and disabled when a usable previous HZB is absent. Nanite geometry support and programmable raster features vary by material, platform and configuration.

Related types: Nanite::FRasterContext, Nanite::FRasterResults.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteCullRaster.cpp:7008` — `CULLING_PASS_OCCLUSION_MAIN`
- `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteCullRaster.cpp:7071` — `CULLING_PASS_OCCLUSION_POST`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:1449` — `void FDeferredShadingSceneRenderer::RenderNanite`

### 08. Lumen cards & Surface Cache (lumen-scene)

Maintain a persistent surface parameterization and capture material properties for the surfaces selected by the update budget.

Lane: caches; column: 5; requires: lumen; async-capable grouping: False.

Major prerequisites: scene-update, views.

**Inputs**

1. Scene primitive changes, Lumen card descriptions and camera priorities
2. Surface-cache atlas allocations and capture geometry

**Outputs**

1. Updated cards/page mappings
2. Albedo, normal, emissive and depth material atlases

**How it works**

1. Update Lumen primitive groups and mesh-card representations as geometry enters, moves or leaves the maintained scene.
2. Prioritize visible/important surface pages and allocate or recycle atlas space within budgets.
3. Capture surface material attributes into card pages; Nanite accelerates supported captures but is not the entire Lumen representation.
4. Update borders/mappings and invalidate stale cached lighting for changed surfaces. Coverage and freshness are independent correctness concerns.

**Operations to implement**

1. Offline card generation or an equivalent surface parameterization
2. Scene membership, spatial prioritization and per-frame update budgeting
3. Virtual page allocation and atlas management
4. Material/depth capture, border dilation and mapping-table updates
5. Invalidation for geometry/material/residency changes

**Correctness checks**

1. Visualize uncached surfaces and card coverage in concave/interior geometry.
2. Move geometry and change emissive/material values; check updates converge without stale illumination.

Configuration/order caveat: The timeline groups work by dependency; Lumen scene setup begins earlier in Render and can overlap geometry preparation. Not every surface is recaptured every frame.

Related types: FLumenSceneData, FLumenMeshCards, FLumenCard, FLumenSurfaceCacheAllocator.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:2894` — `UpdateLumenScene(`
- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenSceneRendering.cpp:3097` — `DilateCardPageOneTexel`

### 09. Light the Lumen Surface Cache (lumen-lighting)

Update direct lighting and radiosity on cached surface pages before their radiance is sampled by later gathers.

Lane: caches; column: 6; requires: lumen; async-capable grouping: True.

Major prerequisites: lumen-scene, trace-scene, atmosphere-luts.

**Inputs**

1. Lumen cards and material atlases
2. Light data, tracing resources and existing cached lighting

**Outputs**

1. Updated direct/indirect cached surface lighting
2. Lighting update state and atlas data

**How it works**

1. Choose pages for lighting updates using separate direct-light and radiosity budgets.
2. Evaluate direct illumination/visibility for selected Lumen scene surfaces.
3. Update radiosity using available scene/cache information and previous state, then make cached radiance available to rays.
4. In this 5.8.1 deferred entry point, RenderLumenSceneLighting is called before RenderBasePass. This is separate from the later screen-space final gather.

**Operations to implement**

1. Light lists, lighting-page selection and indirect work arguments
2. Surface-space direct-light evaluation and visibility traces
3. Radiosity transport, cache sampling and temporal update state
4. Atlas read/write dependencies, invalidation and convergence diagnostics

**Correctness checks**

1. Toggle a light in an enclosed room; track direct and bounced-light convergence separately.
2. Check update budgets and invalidation avoid permanent stale lighting.

Related types: FLumenSceneData.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3041` — `RenderLumenSceneLighting(`
- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenSceneLighting.cpp:293` — `RenderDirectLightingForLumenScene`
- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenSceneLighting.cpp:300` — `RenderRadiosityForLumenScene`

### 10. Base pass & Nanite materials (gbuffer)

Evaluate visible surface materials into deferred shading inputs shared by conventional meshes and Nanite.

Lane: geometry; column: 7; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: depth, nanite-visibility.

**Inputs**

1. Depth/visibility, mesh commands and material shaders
2. GPU Scene, textures, DBuffer decals where enabled

**Outputs**

1. GBuffer or configured Substrate data
2. Initial scene color/emissive and optional velocity
3. Resolved depth and material classification

**How it works**

1. Apply pre-base-pass DBuffer decal inputs when configured; these modify material evaluation rather than being a separate light source.
2. Conventional mesh base-pass draws evaluate material and vertex-factory permutations against scene depth.
3. Nanite decodes visibility, bins pixels/tiles by shading work and evaluates the corresponding materials. Nanite's geometry visibility does not bypass material shading.
4. Write normals, material parameters and the selected shading representation; refresh scene-texture bindings for later consumers.

**Operations to implement**

1. Deferred surface schema and material compiler/permutation cache
2. Depth-tested conventional material draws
3. Nanite visibility decode, shading-bin count/reserve/scatter and indirect material dispatch
4. GBuffer/Substrate writes, emissive accumulation and velocity routing
5. DBuffer inputs, stencil/material classification and resource transitions

**Correctness checks**

1. Render the same material with conventional and Nanite geometry and compare surface outputs.
2. Verify normal spaces, roughness conventions, masked edges, decals and motion vectors before debugging lighting.

Configuration/order caveat: Substrate changes material storage/evaluation details. The stage shows the shared contract, not one universal fixed GBuffer layout. The Nanite dependency is conditional; conventional geometry remains when Nanite is off.

Related types: FMaterial, FVertexFactory, FNaniteShadingCommands, FSceneTextures.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3059` — `RenderBasePass(*this, GraphBuilder, Views, SceneTextures`
- `Engine/Source/Runtime/Renderer/Private/Nanite/NaniteShading.cpp:367` — `ShadingBinBuildCS`
- `Engine/Source/Runtime/Renderer/Private/BasePassRendering.cpp:924` — `void FDeferredShadingSceneRenderer::RenderBasePass(`

### 11. Lumen screen-probe final gather (gi)

Estimate indirect diffuse and rough-specular lighting for visible surfaces using screen probes, ray tracing and reusable radiance.

Lane: lighting; column: 8; requires: lumen; async-capable grouping: True.

Major prerequisites: gbuffer, lumen-lighting, trace-scene.

**Inputs**

1. Valid scene depth/GBuffer and view data
2. Lumen scene lighting, Radiance Cache, trace scene and prior histories

**Outputs**

1. Filtered/integrated indirect lighting textures
2. Probe/radiance-cache histories and confidence information

**How it works**

1. Place screen probes on a regular grid and add adaptive samples where depth/normal variation demands more coverage.
2. Mark/update world-space Radiance Cache probes and generate importance-sampled probe rays.
3. Trace screen-space rays first when enabled; compact unresolved work. Off-screen or invalid screen hits require fallback tracing.
4. Select hardware triangle tracing or software mesh-SDF/heightfield tracing followed by global-SDF coverage as configured. Hardware mode still has a cache/sky completion phase; it does not automatically run software GDF ray marching.
5. Resolve hit radiance from the selected lighting path/cache, filter probe results, interpolate/integrate for pixels and validate/reproject temporal history.

**Operations to implement**

1. Screen/adaptive probe placement, tile classification and sampling sequences
2. Radiance-cache probe allocation, update scheduling and interpolation
3. Screen-depth traversal, hit validation and unresolved-ray compaction
4. SDF sphere tracing or hardware ray dispatch with synchronized AS
5. Radiance lookup, importance sampling, spatial/temporal filtering and per-pixel integration

**Correctness checks**

1. Reveal an off-screen bright source and verify fallback tracing still contributes.
2. Test camera cuts, disocclusion, thin walls and roughness variation; visualize probe coverage and history rejection.

Configuration/order caveat: Screen Probe Gather is the main illustrated final-gather path; experimental/alternative modes and quality settings can change the pass set. Async dispatch happens only after required inputs are valid.

Related types: FScreenProbeGatherParameters, FRadianceCacheState.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenScreenProbeGather.cpp:2624` — `GenerateImportanceSamplingRays`
- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenScreenProbeGather.cpp:2647` — `TraceScreenProbes`
- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenScreenProbeGather.cpp:2711` — `InterpolateAndIntegrate`
- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenScreenProbeTracing.cpp:918` — `TraceGlobalSDFAndApplyRadianceCache`

### 12. Water & front-layer receiver depth (receiver-depth)

Provide the extra receiver information needed before virtual shadow-map page requests.

Lane: geometry; column: 8; requires: vsm; async-capable grouping: False.

Major prerequisites: gbuffer.

**Inputs**

1. Opaque scene depth, water/front-layer materials and view data

**Outputs**

1. Single-layer-water depth when enabled
2. Front-layer translucency receiver data for VSM marking

**How it works**

1. Render the selected single-layer-water depth prepass. Its before/after-base-pass location is configurable; the late option must still finish before VSM requests.
2. Prepare eligible front-layer translucency receiver data with the VSM-page-marking path.
3. Pass these depth/receiver resources into virtual-page marking alongside opaque scene depth.
4. Final water shading and front-layer/translucent composition happen later; receiver setup is not final transparent color.

**Operations to implement**

1. Water depth material permutations and target/resolve handling
2. Front-layer depth/receiver classification and resources
3. Pass dependencies from receiver writes to VSM page marking

**Correctness checks**

1. Place water and glass receivers where opaque depth alone would not request the right shadow pages.
2. Compare water depth-prepass locations and verify final shadow coverage stays correct.

Configuration/order caveat: This node shows the latest required receiver-preparation window for VSM. A before-base-pass water prepass can produce its portion earlier. Each resource is conditional on the relevant material/feature being enabled.

Related types: FSceneTextures.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:2888` — `RenderSingleLayerWaterDepthPrepass(`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3221` — `RenderSingleLayerWaterDepthPrepass(`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3272` — `RenderFrontLayerTranslucency(`

### 13. Lumen reflection trace & resolve (reflections)

Trace view-dependent specular rays and reconstruct stable reflections for eligible materials.

Lane: lighting; column: 9; requires: lumen; async-capable grouping: True.

Major prerequisites: gbuffer, trace-scene, lumen-lighting.

**Inputs**

1. Depth, normals, roughness and view direction
2. Screen history, trace scene, Surface Cache and reflection history

**Outputs**

1. Resolved/denoised reflection radiance
2. Updated reflection history

**How it works**

1. Classify eligible pixels/tiles by material and roughness; allocate compact trace work.
2. Use screen traces where allowed and trace unresolved rays in the configured software or hardware representation.
3. Surface-cache mode reads cached hit lighting. Hit Lighting evaluates lighting at ray hits where that mode is supported/enabled; hardware tracing alone does not imply Hit Lighting.
4. Resolve samples to pixels, reject invalid history, temporally accumulate and spatially filter before specular composition.

**Operations to implement**

1. Reflection tile classification, ray generation and indirect dispatch arguments
2. Screen traces, hit validation, fallback tracing and ray/material lookup
3. Surface-cache radiance sampling or hit-lighting shaders and visibility
4. Sample resolve, temporal reprojection/rejection, variance and spatial filtering

**Correctness checks**

1. Use a mirror reflecting an off-screen object and compare tracing/lighting modes.
2. Change camera, roughness, dynamic lighting and resolution; reject stale reflections without persistent trails.

Configuration/order caveat: Diffuse GI and reflection methods are independently selectable in Unreal. The explorer's Lumen switch represents both enabled together; it does not model every mixed-method combination.

Related types: FSceneViewState, FRayTracingScene.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenReflections.cpp:1215` — `FDeferredShadingSceneRenderer::RenderLumenReflections(`
- `Engine/Source/Runtime/Renderer/Private/Lumen/LumenReflections.cpp:683` — `FLumenReflectionResolveCS`

### 14. VSM / conventional shadow depth (shadows)

Produce light-space visibility, with virtual shadow maps allocating depth only for requested pages.

Lane: geometry; column: 9; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: gbuffer, receiver-depth, nanite-visibility.

**Inputs**

1. Visible receiver depth, lights and caster geometry
2. VSM page tables, cache state and invalidation

**Outputs**

1. Resident shadow-depth pages or conventional shadow maps
2. Shadow sampling/page metadata

**How it works**

1. For VSM, mark receiver-requested pages from scene depth and relevant water/front-layer translucency inputs.
2. Allocate/reuse physical pages, invalidate pages affected by moving geometry/lights and prepare clipmap/local-light views.
3. Cull/rasterize Nanite and conventional shadow casters into requested dirty pages.
4. Make depth/page tables available for shadow projection and light evaluation. Conventional shadow maps use a different allocation/view scheme.

**Operations to implement**

1. Light-space projection and directional clipmap/local-light views
2. Virtual page marking, physical allocation, caching and invalidation
3. Shadow caster culling, Nanite shadow views and conventional depth draws
4. Depth atlas writes, page-table lookup, filtering and visibility projection

**Correctness checks**

1. Move a caster and verify old cached shadows disappear while new pages update.
2. Test clipmap boundaries, physical-page exhaustion, water and translucent receivers.

Configuration/order caveat: The earlier conventional-shadow scheduling option is explicitly unsupported with VSM in this path. Do not move VSM page marking ahead of its receiver-depth dependencies. VSM and Nanite are complementary, separately controlled systems.

Related types: FVirtualShadowMapArray, FVirtualShadowMapArrayCacheManager, FVirtualShadowMapClipmap, FProjectedShadowInfo.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3273` — `BeginMarkVirtualShadowMapPages`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3291` — `RenderShadowDepthMaps(`
- `Engine/Source/Runtime/Renderer/Private/VirtualShadowMaps/VirtualShadowMapArray.cpp:3227` — `void FVirtualShadowMapArray::BuildPageAllocations(`

### 15. Late decals, AO & velocity (decals-ao)

Finish the deferred surface/depth inputs needed by lighting, accounting for feature-dependent scheduling.

Lane: geometry; column: 10; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: gbuffer.

**Inputs**

1. Base-pass GBuffer/depth/stencil
2. Decal lists, velocity policy and AO settings

**Outputs**

1. Lighting-ready scene textures
2. AO and motion vectors for relevant consumers

**How it works**

1. Process deferred decals and configured ambient-occlusion work after the base pass; preserve lighting-channel data before stencil reuse where needed.
2. Finish depth resolves and opaque velocity if the selected mode did not produce it earlier.
3. Refresh scene texture uniform parameters after writes.
4. Stochastic lighting can move before-lighting decals earlier so classification and async Lumen read valid material data. AO/lightmap/DFAO interactions depend on the selected lighting path.

**Operations to implement**

1. Projected decal volume draws and blending rules
2. Screen-space AO generation/filtering when enabled
3. Depth resolves, lighting-channel extraction and stencil ownership
4. Late velocity draws and read-after-write dependency declaration

**Correctness checks**

1. Check decals alter the values consumed by both direct and indirect lighting.
2. Test moving opaque geometry and verify AO does not incorrectly double-darken indirect lighting.

Configuration/order caveat: This is a grouping of conditional finishing work, not a barrier that every Lumen dispatch must follow. In particular, 5.8.1 can dispatch async Lumen before the regular late composition call after preparing the inputs it needs.

Related types: FDeferredDecalProxy, FSceneTextures.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3198` — `CompositionLighting.ProcessAfterBasePass`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3391` — `CompositionLighting.ProcessAfterBasePass`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3248` — `DispatchAsyncLumenIndirectLightingWork`

### 16. Deferred direct lighting (direct)

Evaluate lights against visible material data and apply their shadow visibility into HDR scene color.

Lane: lighting; column: 11; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: shadows, decals-ao.

**Inputs**

1. Lighting-ready GBuffer/depth
2. Light data, shadows and scene color

**Outputs**

1. Direct-lit HDR scene color
2. Auxiliary translucency-lighting data where enabled

**How it works**

1. Build/use light lists and select suitable deferred light evaluation paths.
2. Read material/shading-model data, reconstruct surface position and evaluate the light BRDF.
3. Apply shadow visibility, attenuation and light functions and accumulate radiance.
4. Optional MegaLights/stochastic lighting changes light sampling and shadow requests; it is a separate feature, not a synonym for Lumen.

**Operations to implement**

1. Light upload, classification and light-volume/tile/cluster work
2. Position reconstruction, BRDF evaluation and light attenuation
3. Shadow-map/VSM sampling or configured ray-traced shadow evaluation
4. HDR accumulation, light functions and optional translucency-volume injection

**Correctness checks**

1. Compare a single unshadowed light with an analytic BRDF reference.
2. Test shadowed light overlap, energy scaling, lighting channels and exposure independence.

Related types: FLightSceneProxy, FLightSceneInfo.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3467` — `RenderLights(`
- `Engine/Source/Runtime/Renderer/Private/LightRendering.cpp:1627` — `FDeferredShadingSceneRenderer::RenderLights`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3471` — `RenderMegaLights(`

### 17. Indirect & specular composition (composite)

Join indirect work with direct lighting, then complete deferred reflections/sky and material-specific composition.

Lane: lighting; column: 12; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: direct, gi, reflections.

**Inputs**

1. Direct-lit HDR scene color
2. Completed diffuse/rough-specular GI and reflection textures
3. GBuffer, sky/reflection inputs and material classification

**Outputs**

1. Combined opaque HDR lighting
2. Material-specific post-lighting results

**How it works**

1. Non-async indirect work may be launched/composited through the earlier RenderDiffuseIndirectAndAmbientOcclusion call.
2. For regular async Lumen, the renderer performs a dedicated composite after RenderLights so direct lighting can overlap the gather.
3. Join async work through RDG resource dependencies before reading its outputs; this is a real producer/consumer requirement.
4. Compose deferred reflections and sky lighting, then run enabled subsurface/Substrate-specific opaque work without counting the same contribution twice.

**Operations to implement**

1. Async-to-graphics synchronization and declared resource dependencies
2. Diffuse/rough-specular integration and AO/material weighting
3. Specular reflection/sky composition with method-dependent selection
4. Subsurface profiles/filtering and enabled material-specific composition

**Correctness checks**

1. Compare async on/off images under identical settings and inspect queue waits.
2. Isolate direct, diffuse indirect and specular terms to find double counting or missing energy.

Configuration/order caveat: Horizontal position describes an explanatory scheduling window, not milliseconds. Async capability depends on platform, mode and resource hazards; it can be slower when the GPU is already saturated.

Related types: FRDGBuilder, FSceneTextures.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3523` — `/* bCompositeRegularLumenOnly = */ true`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3528` — `RenderDeferredReflectionsAndSkyLighting(`
- `Engine/Source/Runtime/Renderer/Private/IndirectLightRendering.cpp:990` — `DispatchAsyncLumenIndirectLightingWork`

### 18. Sky, fog, clouds & water (atmosphere)

Compose participating media and water with opaque lighting using feature-specific depth and ordering rules.

Lane: output; column: 13; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: composite.

**Inputs**

1. Opaque HDR scene color/depth and lights
2. Atmosphere LUTs, fog/cloud/water resources and histories

**Outputs**

1. Atmospheric/water scene color
2. Depth/transmittance inputs for later translucency

**How it works**

1. Generate sky-atmosphere LUTs earlier according to the selected scheduling mode; they can be required before Lumen scene lighting.
2. Evaluate sky atmosphere, height/volumetric fog and cloud rendering/composition when enabled.
3. Single-layer water uses dedicated depth/refraction/lighting work; its depth prepass can contribute to earlier VSM requests.
4. Underwater translucency and cloud composition have special ordering paths; the ordinary opaque-to-translucent sequence is not universal.

**Operations to implement**

1. Atmosphere transmittance/multiple-scattering LUTs and sky evaluation
2. Froxel volume allocation, light injection, scattering integration and temporal filtering
3. Cloud ray marching, low-resolution reconstruction and composition
4. Water depth, refraction, absorption/scattering and reflection composition

**Correctness checks**

1. Test camera transitions above/below water and fogged opaque silhouettes.
2. Check transmittance and compositing order with clouds and translucent objects.

Configuration/order caveat: This box summarizes optional systems. LUT generation and some async cloud work occur earlier than final composition; rendering without these features skips the corresponding work.

Related types: FSceneTextures.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3784` — `RenderSkyAtmosphere(`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3822` — `RenderFog(`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3880` — `RenderSingleLayerWater(`

### 19. Translucency & scene-color effects (translucency)

Render transparent surfaces with separate shading/compositing rules and pass placement.

Lane: output; column: 14; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: atmosphere.

**Inputs**

1. Opaque/media scene color, scene depth and lights
2. Translucent mesh batches/materials and optional front-layer resources

**Outputs**

1. Composited color or separate translucency targets
2. Translucent velocity/depth where supported

**How it works**

1. Classify translucent draws into configured pass groups and sort where the blend model requires it.
2. Shade using the selected translucency lighting method; transparent materials do not simply write the opaque GBuffer.
3. Use front-layer reflection/depth paths where supported, and composite separate translucency at the appropriate post-processing point.
4. Respect depth, refraction, fog and velocity policies; material coverage differs from opaque Lumen/Nanite paths.

**Operations to implement**

1. Translucent pass classification, draw sorting and blending
2. Translucency lighting-volume or forward surface-lighting evaluation
3. Scene-color copy/refraction and separate target allocation
4. Depth-aware composition and supported translucent motion vectors

**Correctness checks**

1. Test overlapping glass, fog, refractive surfaces and fast motion.
2. Check before/after-DOF placement and front-layer reflection limitations.

Configuration/order caveat: Main opaque Nanite/Lumen behavior should not be generalized to every translucent material. Some translucency is rendered earlier (for example underwater/front-layer setup) or composited later.

Related types: FMeshBatch, FMaterial.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:4011` — `RenderTranslucency(`
- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:3496` — `RenderFrontLayerTranslucency(`

### 20. Temporal reconstruction & post (post)

Convert HDR scene radiance into a stable, display-ready image while managing exposure and temporal histories.

Lane: output; column: 15; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: translucency.

**Inputs**

1. HDR scene color, depth, velocity and translucency resources
2. Previous temporal/exposure history, jitter and output size

**Outputs**

1. Upscaled/anti-aliased, tone-mapped output
2. New view histories and exposure state

**How it works**

1. Run enabled depth-of-field and pre-upscale material/translucency work.
2. Select TSR, TAA or a configured third-party temporal upscaler, or a non-temporal AA path. Reproject with motion/depth and reject invalid history.
3. Apply enabled motion blur and construct exposure/bloom inputs; precise pass choices vary with settings and extensions.
4. Tone map/color grade for the output encoding, then apply configured after-tonemap effects, spatial AA or output scaling.
5. Extract valid history resources and mark resets for camera cuts, resolution/method changes and unsupported reuse.

**Operations to implement**

1. Temporal jitter, reprojection, disocclusion detection, history clamping and reconstruction
2. Dynamic-resolution input/output rect handling and history allocation
3. DOF/motion-blur filters and translucency integration
4. Exposure measurement/adaptation, bloom pyramid and tone/color transform
5. Output encoding plus history extraction/reset policy

**Correctness checks**

1. Test camera cuts, disocclusion, dynamic resolution and thin moving geometry.
2. Verify linear HDR lighting is not accidentally tone-mapped twice; test SDR/HDR output conventions.

Configuration/order caveat: TSR reconstructs the image from current/previous inputs; it does not generate Nanite geometry or Lumen GI. The post chain is conditional and extensible, not one fixed list for every view.

Related types: FSceneViewState, FRDGTexture.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/DeferredShadingRenderer.cpp:4311` — `AddPostProcessingPasses(`
- `Engine/Source/Runtime/Renderer/Private/PostProcess/PostProcessing.cpp:1109` — `UpscalerPassInputs`
- `Engine/Source/Runtime/Renderer/Private/PostProcess/PostProcessing.cpp:1629` — `AddTonemapPass(`

### 21. RDG execution, history & output (execute)

Compile and execute the declared frame graph, retain requested history and hand completed output to the viewport path.

Lane: output; column: 16; requires: baseline/conditional subfeatures; async-capable grouping: False.

Major prerequisites: post.

**Inputs**

1. Pass/resource declarations from the whole frame
2. External targets, extracted histories and queue capabilities

**Outputs**

1. Submitted GPU commands and render target output
2. Retained histories, feedback/readbacks and resource retirement

**How it works**

1. Render functions mostly declare RDG passes. Track reads/writes and dependencies throughout all earlier stages, not only at this final box.
2. Compile the graph: cull unused work, determine lifetimes, allocate transient resources and produce transitions/queue synchronization.
3. SceneRenderBuilder executes the graph. RHI command recording/submission carries actual GPU work to the backend.
4. Extract histories and feedback for future frames; imported/extracted resources have lifetimes beyond an individual graph.
5. Viewport/swapchain code handles presentation outside FDeferredShadingSceneRenderer::Render. Completion/fences govern reuse and destruction.

**Operations to implement**

1. Typed resources/views, pass parameters, access tracking and dependency DAG
2. Pass culling, lifetime analysis, transient allocation/aliasing and barrier generation
3. Graphics/compute queue submission, cross-queue waits and GPU fences
4. External-resource import/export, history extraction and readback staging
5. Swapchain output transitions, presentation and frames-in-flight retirement

**Correctness checks**

1. Poison/reuse transient memory to expose undeclared dependencies.
2. Compare serialized and async schedules; check graph resources are not retained as dangling FRDG pointers.
3. Resize/recreate the viewport while frames are in flight and validate fence-controlled resource retirement.

Configuration/order caveat: The last column indicates submission/output responsibility, not that earlier boxes were already executing on the GPU as the CPU declared them. RDG logical objects and native GPU allocations are different lifetime layers.

Related types: FRDGBuilder, FRDGPass, FRHICommandListImmediate, FRHIGPUFence.

Source anchors:

- `Engine/Source/Runtime/Renderer/Private/SceneRenderBuilder.cpp:772` — `GraphBuilder.Execute();`
- `Engine/Source/Runtime/RenderCore/Private/RenderGraphBuilder.cpp:1766` — `FRDGBuilder::Execute`

## Reimplementation roadmap

This is an inferred engineering plan, not an inventory of existing Ardashir features. Each gate is a proposed correctness criterion, not a claim that an Unreal-equivalent renderer has been implemented or benchmarked.

### 1. Correct conventional frame

Implement resource/view/PSO/command contracts, uploads, frame retirement and a dependency graph. Add scene registration, views, depth, GBuffer, one direct light, conventional shadows, tone mapping and presentation.

Exit gate: A fixed test scene matches a CPU/analytic reference; resize and add/remove operations survive multiple frames in flight.

### 2. GPU-driven scene

Add structured primitive/instance tables, stable handles, cached mesh commands, GPU culling, indirect work and current/previous transform history.

Exit gate: CPU and GPU culling agree; material changes invalidate commands; moving instances have correct velocities.

### 3. Virtualized geometry

Build an offline cluster/page pipeline, streaming residency, hierarchy traversal, hardware/software visibility raster, HZB recovery and material shading bins. Preserve the conventional path for unsupported content.

Exit gate: Streaming pressure and rapid disocclusion never produce persistent holes; visibility and material outputs agree with a reference mesh.

### 4. Cached dynamic indirect light

Implement a trace scene (SDF or AS), card/surface-cache parameterization, capture and lighting updates; add screen/radiance probes, fallback tracing and diffuse/specular resolve with temporal rejection.

Exit gate: Off-screen illumination works; changed lights/materials converge; camera cuts and disocclusion do not retain stale lighting.

### 5. Scalable shadows and composition

Add VSM request/allocation/invalidation, full light classification, reflection composition, material-specific effects, atmosphere/water and translucent pass policies.

Exit gate: Cache invalidation and depth dependencies hold across moving lights/casters and transparent/media scenes.

### 6. Reconstruction and overlap

Add robust TSR-like reconstruction or integrate an upscaler, then tune update budgets, async compute, transient aliasing and streaming from captures.

Exit gate: Async and serial images agree within algorithmic tolerance; measured critical-path time improves without residency or temporal regressions.

## Questions and distinctions

### What is the ownership path from a static mesh to a draw?

AActor owns components; UStaticMeshComponent references UStaticMesh asset data and creates a suitable FPrimitiveSceneProxy (ordinary FStaticMeshSceneProxy or supported Nanite proxy). FScene registers a FPrimitiveSceneInfo, and FGPUScene exposes shader-readable primitive/instance data. Conventional passes consume FMeshBatch through FMeshPassProcessor into FMeshDrawCommand; Nanite consumes its cluster/page representation and raster/shading pipelines. These arrows describe association/data flow, not C++ inheritance.

### Which objects actually live on the GPU?

UObjects, scene proxies, FScene, FRDGBuilder and FRHIResource-derived wrappers are CPU-side management objects. GPU buffers/textures/AS hold their uploaded or generated data. FRenderResource manages render-resource initialization/release; RHI wraps backend resources; RDG describes logical use and frame lifetimes. Do not upload arbitrary C++ proxy object memory as a shader ABI.

### How do RDG resources survive a frame?

Import existing external resources into a graph and queue extraction when an output must persist. Store the extracted backing resource in view/scene state and register it in the next graph. A raw FRDGTexture/FRDGBuffer pointer is scoped to its graph and is not a persistent GPU allocation handle.

### Are Nanite, VSM and Lumen one mandatory pipeline?

No. Nanite virtualizes geometry, VSM virtualizes shadow-depth storage, and Lumen estimates indirect diffuse/specular lighting. They share scene/depth/geometry data and can accelerate one another, but have distinct representations and feature switches. Conventional geometry still participates in this deferred frame.

### Why can the timeline place Lumen before and after the base pass?

Lumen scene/card updates and cached surface lighting maintain a persistent scene representation and are scheduled before the main base pass in this checkout. Screen-probe final gather and reflection work need visible-surface inputs afterward. Regular async Lumen composition occurs after direct lighting to permit overlap. This does not put all Lumen work in one serial slot.

### Is the type inventory exhaustive?

The general resource reference is a lexical index of its declared runtime module/header scope. Separate primitive discovery follows explicit C++ subclasses of UPrimitiveComponent and FPrimitiveSceneProxy across runtime and plugin source, including optional and some editor helper definitions. Neither is a compiler AST: aliases, unsupported declaration macros, forward declarations and template instantiations can remain unparsed. Inactive conditional definitions remain explicit. The two primitive trees preserve real inheritance; GPU storage is a separate reviewed family association. Other subclasses show their nearest mapped base as context, not a guaranteed subclass storage layout. See the primitive reference for exact scope and source identities.

### What does this say about existing Ardashir capabilities?

The operation lists and roadmap describe work needed for a renderer with similar responsibilities. They do not assert that current Ardashir modules already implement those features. Exact shaders, binary layouts, platform support, quality settings and production performance require further engineering and source study.

## Further official reading

- [Lumen technical details](https://dev.epicgames.com/documentation/en-us/unreal-engine/lumen-technical-details-in-unreal-engine)
- [Nanite virtualized geometry](https://dev.epicgames.com/documentation/en-us/unreal-engine/nanite-virtualized-geometry-in-unreal-engine)
- [Mesh drawing pipeline](https://dev.epicgames.com/documentation/en-us/unreal-engine/mesh-drawing-pipeline-in-unreal-engine)
